## 1. Setup + dependencies

- [x] 1.1 Add runtime dep `typer = ">=0.12,<1.0"` to root `pyproject.toml`. (`openapi-typescript` is already in root `package.json` from slice 1; verify the version is `^7.0` and bump if needed.)
- [x] 1.2 Add devDep `@lhci/cli = "^0.14"` to root `package.json` for the Lighthouse CI step. Workspace install handles propagation.
- [ ] 1.3 Lock root `poetry.lock` via the `regenerate-lock.yml` workflow_dispatch + commit (per gotcha #18 the local poetry path is fragile). Pull the regenerated lock back to the slice branch before continuing.
- [x] 1.4 Lock root `pnpm-lock.yaml` via `pnpm install` (works locally) and commit.

## 2. Backend — error rendering + dynamic discovery

- [ ] 2.1 Create `apps/api/src/iguanatrader/api/errors.py` with `register_error_handlers(app: FastAPI) -> None` exposing two handlers: `_render_problem(request, exc: IguanaError)` (renders RFC 7807 via `exc.to_problem_dict()` + `media_type="application/problem+json"`) and `_render_internal(request, exc: Exception)` (re-raises `HTTPException`/`RequestValidationError` to FastAPI defaults; wraps everything else as `InternalError` and emits structlog `api.unhandled_exception` with `exc_info=True`).
- [ ] 2.2 Add `BootstrapNotReadyError(IguanaError)` to `apps/api/src/iguanatrader/shared/errors.py` with `default_status=503`, `default_title="Service Not Bootstrapped"`, `type_uri="urn:iguanatrader:error:not-bootstrapped"`. Re-export from `shared/__init__.py`. Document inline why this addition is post-hoc (slice 4 inline 503 canonicalisation per design D9).
- [ ] 2.3 Create `apps/api/src/iguanatrader/api/dtos/common.py` with Pydantic v2 `Problem` model (fields: `type: str`, `title: str`, `status: int`, `detail: str | None = None`, `instance: str | None = None`, `errors: list[ErrorDetail] | None = None`) and `ErrorDetail` (fields: `field: str | None = None`, `code: str`, `detail: str | None = None`). Set `model_config = ConfigDict(extra="allow")` per RFC 7807 §3.2 extensibility.
- [ ] 2.4 Refactor `apps/api/src/iguanatrader/api/app.py::create_app()` to call `_register_routers(app)` + `_register_sse(app)` + `register_error_handlers(app)` in place of the manual `app.include_router(auth_router, prefix="/api/v1")`. The dynamic-discovery helper is in `iguanatrader.api.routes.__init__::register_routers` (next task).
- [ ] 2.5 Update `apps/api/src/iguanatrader/api/routes/__init__.py` with `register_routers(app: FastAPI) -> None` that runs `pkgutil.iter_modules(__path__)`, imports each module, and `app.include_router(module.router, prefix="/api/v1")` for each that exports a top-level `router`. Emit structlog `api.router.registered` / `api.router.skipped` / `api.router.import_failed` per spec.
- [ ] 2.6 Create `apps/api/src/iguanatrader/api/sse/__init__.py` with `register_sse(app: FastAPI) -> None` mirroring `register_routers` but with prefix `/api/v1/stream` and event names `api.sse.registered` etc.
- [ ] 2.7 Refactor `apps/api/src/iguanatrader/api/routes/auth.py`: replace each `_problem_response(...)` call with the appropriate `raise <IguanaError-subclass>(...)`. Drop the `_problem_response` helper entirely. Zero-tenant 503 becomes `raise BootstrapNotReadyError(detail=...)`. Failed-login 401 becomes `raise AuthError(detail="Invalid email or password.")`. Imports updated.

## 3. Backend — CLI scaffolding

- [ ] 3.1 Create `apps/api/src/iguanatrader/cli/__init__.py` (empty package marker; docstring explains the auto-discovery contract — modules under this package MUST export `app: typer.Typer` to be picked up).
- [ ] 3.2 Create `apps/api/src/iguanatrader/cli/main.py` with `cli_app: typer.Typer` built via `_register_subcommands(cli_app)` that iterates `pkgutil.iter_modules(iguanatrader.cli.__path__)` (excluding `main`), imports each module, and `cli_app.add_typer(module.app, name=module.__name__.split(".")[-1].replace("_", "-"))` for each that exports `app: typer.Typer`. Add `--version` callback reading from `importlib.metadata.version("iguanatrader")`.
- [ ] 3.3 Create `apps/api/src/iguanatrader/cli/__main__.py` shim so `python -m iguanatrader.cli` invokes `cli_app()`.
- [ ] 3.4 Add `[tool.poetry.scripts]` entry to root `pyproject.toml`: `iguanatrader = "iguanatrader.cli.main:cli_app"` so `poetry run iguanatrader --help` works once installed.

## 4. Frontend — shared-types package boot

- [ ] 4.1 Create `packages/shared-types/package.json` with `name: "@iguanatrader/shared-types"`, `version: "0.0.0"`, `private: true`, `main: "src/index.ts"` (no build step — buildless TS package per design D6), `license: "Apache-2.0 AND LicenseRef-Commons-Clause"`.
- [ ] 4.2 Create `packages/shared-types/tsconfig.json` extending root config (or stub-equivalent if no root TS config) with `compilerOptions.declaration = false`, `noEmit = true` (consumers resolve TS source directly).
- [ ] 4.3 Create `packages/shared-types/src/index.ts` placeholder with header comment `// Auto-generated by openapi-typescript on every CI run; do not edit by hand. Last regenerated: never (placeholder until first CI run lands the bot commit).` and a single `export {};` so the file is valid TS.
- [ ] 4.4 Update `apps/web/package.json` to declare `"@iguanatrader/shared-types": "workspace:*"` in `dependencies` (consumed in slice W1+; declaring now exercises the workspace symlink + verifies pnpm resolves it).
- [ ] 4.5 Update `apps/web/tsconfig.json` paths so `import { Problem } from '@iguanatrader/shared-types'` resolves (slice W1 will exercise the import).

## 5. CI — typegen + Lighthouse pipeline

- [ ] 5.1 Edit `.github/workflows/openapi-types.yml` to: (a) install pnpm + node + python deps, (b) boot `python -m iguanatrader.api` in background with `IGUANATRADER_JWT_SECRET=$(openssl rand -hex 32)`, (c) curl `http://127.0.0.1:8000/openapi.json` to a temp file, (d) run `pnpm --filter @iguanatrader/shared-types exec openapi-typescript /tmp/openapi.json -o packages/shared-types/src/index.ts`, (e) commit + push the diff via the `regenerate-lock.yml`-style bot pattern (`git diff --quiet` then `git add` + `git commit -m "chore(types): regenerate ..."` + `git push origin HEAD:${{ github.ref }}`).
- [ ] 5.2 Add Lighthouse CI step to the same workflow (or a sibling): boot `pnpm --filter @iguanatrader/web dev` + `lhci autorun` against `http://localhost:5173/login` with `assertions.categories.accessibility = ["error", { minScore: 0.9 }]` and perf/best-practices/seo as informational. Upload `.lighthouseci/` as workflow artefact.
- [ ] 5.3 Add `lighthouserc.cjs` (or `.js`) to repo root with the assertions config + URLs list (`/login` for slice 5; slice W1 will append `/portfolio` etc.).

## 6. Tests

- [ ] 6.1 Create `apps/api/tests/integration/test_error_rendering.py`: pytest-asyncio + httpx ASGITransport client. Tests: `IguanaError` subclass raise → RFC 7807 body shape; unhandled `ValueError` → 500 + Problem; `HTTPException` passes through to FastAPI defaults; `RequestValidationError` (e.g., bad Pydantic body) returns 422 with FastAPI's native shape NOT Problem; `BootstrapNotReadyError` renders with the canonical urn-form type URI.
- [ ] 6.2 Create `apps/api/tests/integration/test_dynamic_discovery.py`: drop a stub `apps/api/src/iguanatrader/api/routes/_test_stub.py` (gitignored or under a tests fixture path) at runtime; assert it gets registered + reachable via the test client. Negative case: drop a module without `router` attribute; assert structlog warning + module skipped. Drop a broken module (raises on import); assert app boot fails loudly.
- [ ] 6.3 Create `apps/api/tests/integration/test_cli_discovery.py`: invoke `python -m iguanatrader.cli --version` via subprocess; assert exit 0 + version string in stdout. Drop a stub `iguanatrader.cli._test_subcommand` module; invoke `python -m iguanatrader.cli --help`; assert the subcommand appears.
- [ ] 6.4 Update existing `apps/api/tests/integration/test_auth_flow.py` to assert the refactored 503 body's `type` is `urn:iguanatrader:error:not-bootstrapped` (was `https://iguanatrader.local/problems/not-bootstrapped` in slice 4). Same body shape, just type URI canonical.

## 7. Documentation

- [ ] 7.1 Append to `docs/gotchas.md`: gotcha #29 — CLI subcommands MUST use lazy imports for heavy deps (numpy, pandas, etc.) so `python -m iguanatrader.cli --version` stays fast. Gotcha #30 — `Exception` fallback handler must re-raise `HTTPException` + `RequestValidationError`; otherwise FastAPI's native 404/422 responses get clobbered into 500 Problem.
- [ ] 7.2 Update `apps/api/README.md`: document `python -m iguanatrader.cli --help` as the discovery entrypoint; document the route-discovery contract (`routes/<name>.py` exporting `router`); document the `pnpm typegen:from-running-api` script as the dev-mode local typegen path (mirrors what CI does).
- [ ] 7.3 Add `docs/runbooks/api-foundation-typegen.md`: when openapi-types.yml fails (e.g., schema generation error), the operator playbook for unblocking — manual local regen + commit, or rebase + retry.

## 8. Pre-merge verification

- [ ] 8.1 `mypy --strict apps/api/src/iguanatrader/api/ apps/api/src/iguanatrader/cli/` clean (covers new code).
- [ ] 8.2 `pytest apps/api/tests/integration/test_error_rendering.py apps/api/tests/integration/test_dynamic_discovery.py apps/api/tests/integration/test_cli_discovery.py apps/api/tests/integration/test_auth_flow.py` all green.
- [ ] 8.3 Coverage on `apps/api/src/iguanatrader/api/{errors,dtos/common,routes/__init__,sse/__init__}.py + iguanatrader/cli/{main,__init__}.py` ≥ 80%.
- [ ] 8.4 `pre-commit run --from-ref origin/main --to-ref HEAD` passes.
- [ ] 8.5 `pnpm --filter @iguanatrader/web check` clean (svelte-check + tsc resolves the new `@iguanatrader/shared-types` import path even though slice W1 hasn't added a real consumer yet).
- [ ] 8.6 Manual smoke: bring up `python -m iguanatrader.api`, confirm `/docs` lists the auth router (still mounted via dynamic discovery), confirm `/openapi.json` contains a `Problem` schema component, confirm `python -m iguanatrader.cli --version` prints the expected string.
- [ ] 8.7 PR description includes "AI-reviewer signoff" subsection per release-management.md §4.5; populate self-review findings + L1 detection result.
