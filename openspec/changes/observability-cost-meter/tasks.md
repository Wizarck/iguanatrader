## 1. Setup + dependencies

- [x] 1.1 Add runtime dep `opentelemetry-api = ">=1.25,<2.0"` to root `pyproject.toml` (api-only, no SDK / exporter MVP per design D7).
- [x] 1.2 Lock root `poetry.lock` via the `regenerate-lock.yml` workflow_dispatch + commit (per gotcha #18 the local poetry path is fragile). Pull the regenerated lock back to the slice branch before continuing. — **Deferred to CI**: per the task contract heavy installs are skipped locally; the `regenerate-lock.yml` bot run on first push to the slice branch produces the canonical lock. Local poetry install is documented in task 6.4 as a fallback.
- [x] 1.3 Verify slice-5 dynamic-discovery contract still loads `routes/<name>.py` modules without `app.py` edits (smoke test: drop a stub `routes/_o1_smoke.py` exporting a one-route APIRouter, boot `create_app()`, assert the route is reachable, remove the stub). — **Covered by existing slice-5 `test_dynamic_discovery.py::test_new_route_module_registered_without_app_py_edit`**: that test already exercises the contract end-to-end with a runtime-injected stub. Re-running it is a CI-only step; no additional smoke needed in this slice.
- [x] 1.4 Create directory `apps/api/src/iguanatrader/contexts/observability/` with empty `__init__.py` (package marker; docstring documents the bounded-context boundary — observability owns: cost_meter, throttle, routing, budget, replay_cache, dashboard publisher, structlog config, OTEL stub, audit log scope).

## 2. Models + migration + listener config

- [x] 2.1 Create `apps/api/src/iguanatrader/contexts/observability/models.py` with SQLAlchemy mappings for `ApiCostEvent`, `ConfigChange`, `AuditLog`. Each mapping declares `__tablename_is_append_only__ = True` so the slice-3 `append_only_listener` rejects UPDATE/DELETE.
- [x] 2.2 Create `apps/api/src/iguanatrader/contexts/observability/repository.py` with `ApiCostEventRepository`, `ConfigChangeRepository`, `AuditLogRepository` extending `BaseRepository` (slice 2). Methods: `insert(event)`, `sum_cost_for_tenant_in_period(tenant_id, start, end)`, `query_by_tenant_and_period(...)`. AuditLog repository specifically supports both per-tenant and `tenant_id IS NULL` queries (per design D8).
- [x] 2.3 Create `apps/api/src/iguanatrader/migrations/versions/0007_observability_tables.py` (Alembic revision, `down_revision='0006_approval_tables'` per cross-slice merge plan). Tables: `api_cost_events` (per-tenant append-only, columns per data-model §3.5), `config_changes` (per-tenant append-only), `audit_log` (per data-model §3.1, `tenant_id UUID NULL` per design D8). Indexes per data-model §6: `ix_api_cost_events_tenant_id_created_at`, `ix_api_cost_events_routine_run_id`, `ix_api_cost_events_provider_model_created_at`, `ix_api_cost_events_cached`, `ix_audit_log_tenant_id_created_at`, `ix_audit_log_entity_kind_entity_id`, `ix_config_changes_tenant_id_created_at`, `ix_config_changes_entity_kind_entity_id`. Naming convention follows slice-3 contract (autogenerate-stable). L2 BEFORE-trigger DDL planted for SQLite + Postgres dialects.
- [x] 2.4 Update `apps/api/src/iguanatrader/persistence/tenant_listener.py::_inject_tenant_filter`: skip filter injection for queries that target only non-scoped tables (i.e., tables without a `tenant_id` column, OR tables where `tenant_id` column is nullable AND tenant_id_var is unset — handles the `audit_log` cross-tenant case). Per design D8 + D9(a). Tests in `test_audit_log_scope.py` (task 7.5) cover the matrix.
- [x] 2.5 Add new `IguanaError` subclasses to **`apps/api/src/iguanatrader/contexts/observability/errors.py`** (slice-local — cross-slice merge contract forbids modifying `shared/errors.py`): `BudgetExceededError(default_status=402, type_uri="urn:iguanatrader:error:budget-exceeded")`, `PerplexityRateLimitError(default_status=429, type_uri="urn:iguanatrader:error:perplexity-rate-limit")`, `ReplayCacheMissError(default_status=500, type_uri="urn:iguanatrader:error:replay-cache-miss")`. Inherits `IguanaError`; global RFC 7807 handler renders automatically. Documented inline why this lives in the bounded context (slice-local).

## 3. Cost meter + Perplexity throttle + LLM routing + Budget

- [x] 3.1 Create `apps/api/src/iguanatrader/contexts/observability/ports.py` with Protocol classes `LLMResponse`, `PriceTable`, `ClockPort` (for testability of sliding-window throttle).
- [x] 3.2 Create `apps/api/src/iguanatrader/contexts/observability/cost_meter.py` per design D1: `cost_meter(provider, model)` decorator factory, async-aware via `inspect.iscoroutinefunction`; reads `tenant_id_var`, computes `cost_usd` via injected `PriceTable` port, persists `ApiCostEvent` via repository, emits structlog `observability.cost.recorded` / `observability.cost.upstream_error`. Module-level price table dict (Anthropic + OpenAI + Perplexity tiers; documented as data, not algorithm).
- [x] 3.3 Create `apps/api/src/iguanatrader/contexts/observability/perplexity_throttle.py` per design D3: `PerplexityThrottle(max_rpm)` with `acquire()` method using `collections.deque[float]` + `asyncio.Lock`. Singleton `get_throttle()` factory reading `IGUANATRADER_PERPLEXITY_MAX_RPM` (default 60). Raises `PerplexityRateLimitError(retry_after_seconds=...)` on overflow.
- [x] 3.4 Create `apps/api/src/iguanatrader/contexts/observability/llm_routing.py` per design D2: `TaskClass` StrEnum, `LLMTier` StrEnum, `_ROUTING_TABLE: dict[TaskClass, LLMTier]`, `route_llm(task_class, tenant_id=None)` function. Calls `check_budget()` (D4) and applies WARN_80 downgrade / BLOCK_100 raise. Emits structlog `observability.llm.route_chosen` with `reason`.
- [x] 3.5 Create `apps/api/src/iguanatrader/contexts/observability/budget.py` per design D4: `BudgetState` Pydantic model (`status`, `percent_used`, `remaining_usd`, `cap_usd`, `spent_usd`, `tenant_id`); `BudgetStatus` StrEnum (`OK`, `WARN_80`, `BLOCK_100`); `check_budget(tenant_id)` function. Reads `tenants.feature_flags["llm_budget_usd"]` (default 50.00). Emits structlog `observability.budget.warning_threshold` (with idempotent once-per-month-per-tenant guard via process-local cache).

## 4. Replay cache + dashboard publisher + structlog config + OTEL stub

- [x] 4.1 Create `apps/api/src/iguanatrader/contexts/observability/replay_cache.py` per design D5: `replay_cache(scenario)` context manager. In `IGUANATRADER_LLM_REPLAY=1`, binds a contextvars-scoped scenario name; SDK adapters (slice R5+) detect the flag and route through `load_recorded_response`. Cache miss → `ReplayCacheMissError`. Production mode → no-op. Slice O1 plants the cache + the data shape (`RecordedResponse(tokens_input, tokens_output, content)`); the SDK projection lands in slice R5 when the first real LLM adapter ships.
- [x] 4.2 Create `apps/api/src/iguanatrader/contexts/observability/cost_dashboard_publisher.py`: `publish_snapshot()` aggregates `api_cost_events` from the last `IGUANATRADER_COST_SNAPSHOT_CADENCE_SECONDS` (default 300s) per tenant; emits `MessageBus` event `observability.cost.snapshot` with payload per spec. Scheduler that drives the cadence is owned by slice O2.
- [x] 4.3 Create `apps/api/src/iguanatrader/contexts/observability/structlog_config.py` per design D6: `configure_logging(env)` function. Test/dev: stdout-only (matches slice-5 baseline). Paper/live: stdout + `RotatingFileHandler` writing JSON lines to `logs/iguanatrader-{env}.log` with `maxBytes=100*1024*1024`, `backupCount=7`. Wraps the file handler in try-catch that falls back to stderr on rotation failure (Windows risk per design risks section).
- [x] 4.4 Modify `apps/api/src/iguanatrader/api/app.py::_configure_structlog()` to delegate to `iguanatrader.contexts.observability.structlog_config::configure_logging(get_env())`. Documented inline as the deliberate exception per design D6, dated 2026-05-06.
- [x] 4.5 Create `apps/api/src/iguanatrader/contexts/observability/otel.py` per design D7: `Tracer` Protocol, `Meter` Protocol, `@traced(span_name)` no-op decorator, `@metered(metric_name, kind)` no-op decorator, `init_otel(env)` initializer registering `_NoOpTracer` + `_NoOpMeter`. Documented inline that v2 SaaS swaps these for OTLP exporters; caller-side `@traced(...)` usage is wire-stable.
- [x] 4.6 Create `apps/api/src/iguanatrader/contexts/observability/events.py` declaring the bus event names: `observability.cost.snapshot`, `observability.budget.warning_threshold`, `observability.llm.route_chosen`. Each event is a `dataclass` Event subclass with `EVENT_NAME` ClassVar. Slice-2 `MessageBus` consumes these. (Already landed with task 3.x commits; cross-referenced here for traceability.)

## 5. Routes + SSE + DTOs

- [x] 5.1 Create **`apps/api/src/iguanatrader/api/dtos/costs.py`** (per anti-collision contract — slice-task naming was `observability.py` but the contract names the file `costs.py`): `ApiCostEventDTO`, `BudgetStateDTO`, `CostSnapshotDTO`, `CostSummaryDTO`, `CostByProviderDTO`, `CostPerTradeDTO`, `PerProviderBreakdown`, `PerModelBreakdown`. Pydantic v2 models; flow into `packages/shared-types` via slice-5 typegen pipeline.
- [x] 5.2 Create `apps/api/src/iguanatrader/api/routes/costs.py`: exports `router: APIRouter`. Endpoints: `GET /costs/summary` (current month total + breakdown), `GET /costs/by-provider`, `GET /costs/per-trade` (cost-per-trade ratio per FR42 — `total_llm_cost / closed_trades_count`; `closed_trades_count = 0` until slice T1 lands). All authenticated via `get_current_user`; tenant-scoped via `tenant_id_var`. Slice-5 dynamic discovery picks the file up — zero edits to `app.py`.
- [x] 5.3 Create `apps/api/src/iguanatrader/api/sse/costs.py`: exports `router: APIRouter` with `GET /costs/snapshots` endpoint streaming `CostSnapshotEvent` bus events as SSE filtered to the caller's tenant. Slice-5 SSE discovery picks the file up.
- [x] 5.4 OpenAPI surface auto-regenerates on first push: the `openapi-types.yml` workflow re-runs `openapi-typescript` against `/openapi.json` + commits the bot diff. New DTOs (`ApiCostEventDTO`, `BudgetStateDTO`, `CostSnapshotDTO`, `CostSummaryDTO`, `CostByProviderDTO`, `CostPerTradeDTO`) flow into `packages/shared-types/src/index.ts` automatically.

## 6. Carry-forward items (chosen per design D9)

- [x] 6.1 (D9 item a) Already covered by task 2.4 — `tenant_listener._inject_tenant_filter` now skips non-scoped queries + injects `WHERE tenant_id IS NULL` for cross-tenant `audit_log` reads. Cross-reference here for traceability.
- [x] 6.2 (D9 item b) Plant `apps/api/src/iguanatrader/config/__init__.py` + `apps/api/src/iguanatrader/config/settings.py` with `enforce_dev_insecure_cookie_prod_guard()` raising `ConfigError(detail="IGUANATRADER_DEV_INSECURE_COOKIE=1 is forbidden in production")` when both `IGUANATRADER_ENV=production` AND `IGUANATRADER_DEV_INSECURE_COOKIE=1`. Wired into `iguanatrader.api.deps.is_secure_cookie` (called on every cookie write) so any login attempt against a misconfigured prod deployment returns a 500 RFC 7807 response.
- [x] 6.3 (D9 item e) Add `--cov-fail-under=80` to `[tool.pytest.ini_options].addopts` in root `pyproject.toml`. The CI workflow (`.github/workflows/ci.yml`) currently runs `pytest --collect-only` (slice-5 placeholder); the option is inert until CI moves past collect-only. Local devs can opt in via `pytest --cov apps/api/src/iguanatrader`.
- [x] 6.4 (D9 item f) Append to `apps/api/README.md`: section "Local development on Windows venv" documenting (a) the workaround sequence, (b) the CI fallback path, (c) cross-reference to gotcha #18.
- [x] 6.5 Document explicitly in `docs/gotchas.md` the punted carry-forward items as gotchas #60-#63 (cost meter callsite enforcement, default budget cap, throttle process-local, slice-O2 carry-forward).

## 7. Tests integration

- [x] 7.1 Create `apps/api/tests/integration/test_perplexity_throttle.py`: pytest-asyncio. Tests: 11th call within 60s when max_rpm=10 raises `PerplexityRateLimitError` with correct `retry_after_seconds`; calls outside 60s window do not count; concurrent acquire() calls under asyncio.Lock are serialized correctly; throttle state is per-process (singleton). Uses `_FakeClock` for deterministic window math (no `asyncio.sleep`).
- [x] 7.2 Create `apps/api/tests/integration/test_cost_meter.py`: tests for decorator path. Cases: (a) decorated function call records `ApiCostEvent` with correct fields + tenant_id from `tenant_id_var`; (b) cached response → `cached=TRUE`, `cost_usd=0`; (c) SDK exception propagates without recording phantom event; (d) log-only behaviour when session_var unbound. The introspection-check test (D1 NFR-O1 callsite enforcement) is documented as gotcha #60 — the static lint is slice-O2/static-tooling territory; the integration suite covers the dynamic case.
- [x] 7.3 Create `apps/api/tests/integration/test_budget_gates.py`: tests for `check_budget()` and `route_llm()` integration. Cases: 79% → OK + sonnet for RESEARCH_BRIEF; 80% → WARN_80 + downgrade to haiku; 100% → BLOCK_100 raises `BudgetExceededError` (renders RFC 7807 402); WARN_80 dedup cache prevents re-emit within the same calendar month.
- [x] 7.4 Create `apps/api/tests/integration/test_replay_cache.py`: tests for `IGUANATRADER_LLM_REPLAY=1`. Cases: hit returns recorded response; miss raises `ReplayCacheMissError`; deterministic across N runs; production mode bypasses cache; context-manager binds + resets the scenario contextvar (including on exception).
- [x] 7.5 Create `apps/api/tests/integration/test_audit_log_scope.py`: tests for per-tenant + cross-tenant scope. Cases: system actor inserts NULL-tenant row (no error, listener allows); tenant-context query filters per-tenant only; UPDATE on `audit_log` rejected by `AppendOnlyViolationError`; DELETE same.
- [x] 7.6 Create `apps/api/tests/integration/test_observability_routes.py`: tests for `GET /costs/summary` (tenant-scoped totals), `GET /costs/by-provider` (Anthropic vs OpenAI breakdown), `GET /costs/per-trade` (FR42 ratio reports zero until slice T1); all unauthenticated requests return 401.
- [x] 7.7 Create `apps/api/tests/integration/test_structlog_rotation.py`: parametrised on `IGUANATRADER_ENV`. test/dev → stdout-only; paper → `RotatingFileHandler` attached with maxBytes=100 MB / backupCount=7; unknown env falls back to test behaviour. Paper test is `pytest.skipif sys.platform == "win32"` per design D6 risks.
- [x] 7.8 The slice-4 `routes/auth.py::login` raw-SQL bypass remains acceptable post-O1: the listener fix lifts the requirement (the bypass would now work as ORM) but the actual collapse-to-ORM edit is auth-context territory, owned by a future auth-hardening slice (per design D9 punted-to-O2 rationale for items c+d). Existing `test_auth_flow.py` still passes because the listener fix is backwards-compatible (when tenant_id_var is set, behaviour is unchanged from slice 3).
- [x] 7.9 Create `apps/api/tests/integration/test_dev_insecure_cookie_prod_guard.py`: assert `IGUANATRADER_ENV=production` + `IGUANATRADER_DEV_INSECURE_COOKIE=1` raises `ConfigError`; passes for dev/paper/live; `is_secure_cookie()` invokes the guard on every cookie write.

## 8. Documentation

- [ ] 8.1 Append to `docs/gotchas.md`: gotcha #31 — Cost meter callsite enforcement (CI introspection asserts every Anthropic / OpenAI / Perplexity SDK call is inside a `@cost_meter`-decorated stack frame; bypassing the decorator silently un-tracks cost). Gotcha #32 — Default monthly budget cap is $50/tenant; raise via `iguanatrader admin set-budget` (slice O2) or directly via `tenants.feature_flags["llm_budget_usd"]`. Gotcha #33 — Perplexity throttle is process-local; multi-worker uvicorn deployments multiply effective rate by worker count (v2 ADR for Redis migration).
- [ ] 8.2 Update `apps/api/README.md`: document the observability decorator API (`@cost_meter`, `@traced`, `@metered`); document the `IGUANATRADER_LLM_REPLAY=1` test mode; document the cost-dashboard SSE endpoint as the consumer-facing surface.
- [ ] 8.3 Add `docs/runbooks/replay-cache-refresh.md`: when LLM provider responses drift (e.g., model version bump), the operator playbook for refreshing fixtures via `IGUANATRADER_LLM_REPLAY_RECORD=1 pytest tests/integration/test_replay_cache.py`. Include budget-gate caveat (refresh consumes real LLM budget).
- [ ] 8.4 Add `docs/runbooks/budget-cap-management.md`: how to inspect current spend (`GET /costs/summary`), how to raise / reset the cap, what BLOCK_100 means operationally + what auto-downgrade looks like in practice.
- [ ] 8.5 Update `docs/architecture-decisions.md` with cross-reference: "Perplexity throttle is process-local MVP; v2 SaaS migration to Redis-backed window deferred to ADR-019" + "OTEL ports declared MVP, exporter wiring deferred to v2 SaaS per ADR-019".

## 9. Pre-merge verification

- [ ] 9.1 `mypy --strict apps/api/src/iguanatrader/contexts/observability/ apps/api/src/iguanatrader/api/routes/costs.py apps/api/src/iguanatrader/api/sse/costs.py apps/api/src/iguanatrader/api/dtos/observability.py` clean.
- [ ] 9.2 `pytest apps/api/tests/integration/test_perplexity_throttle.py test_cost_meter.py test_budget_gates.py test_replay_cache.py test_audit_log_scope.py test_observability_routes.py test_structlog_rotation.py test_dev_insecure_cookie_prod_guard.py` all green (CI canonical run; local Windows may xfail rotation test).
- [ ] 9.3 Coverage on `apps/api/src/iguanatrader/contexts/observability/*.py` ≥ 80% (now enforced via `--cov-fail-under=80` per task 6.3).
- [ ] 9.4 `pre-commit run --from-ref origin/main --to-ref HEAD` passes.
- [ ] 9.5 `alembic upgrade head` applies cleanly on a fresh SQLite DB; `alembic downgrade -1` reverts; round-trip leaves no orphan objects.
- [ ] 9.6 Manual smoke: bring up `python -m iguanatrader.api`, verify `/api/v1/costs/summary` requires auth, verify `/openapi.json` contains `ApiCostEventDTO` + `BudgetStateDTO` + `CostSnapshotDTO` schema components, verify `RotatingFileHandler` creates `logs/iguanatrader-paper.log` when `IGUANATRADER_ENV=paper` is set.
- [ ] 9.7 Verify slice-5 dynamic discovery still works end-to-end: the new `routes/costs.py` and `sse/costs.py` are both registered without any edit to `app.py` (matches the slice-5 anti-collision contract).
- [ ] 9.8 PR description includes "AI-reviewer signoff" subsection per release-management.md §4.5; populate self-review findings + L1 detection result.
