## 1. Setup + scrape-ladder skeleton + politeness primitives

- [x] 1.1 Verify R3 prerequisites are merged on main: R1 `research-bitemporal-schema` archived (check `openspec/changes/archive/2026-05-06-research-bitemporal-schema/` exists); slices 1–5 archived. If any prerequisite is missing, halt with `❓ blocked-by-spec`.
- [x] 1.2 Add new dependencies to `pyproject.toml` under a NEW `[tool.poetry.group.research]` extras group (so the API hot path doesn't pull research-only deps): `playwright>=1.42`, `beautifulsoup4>=4.12`, `lxml>=5.0`, `yfinance>=0.2.40`, `ib_async>=1.0.3`, `tenacity>=8.2`, `google-cloud-bigquery>=3.20`, `2captcha-python>=1.5`. Document each line with a `# why:` comment per slice-1 convention. Run `poetry lock --no-update` and commit `poetry.lock`.
- [x] 1.3 Update `.github/workflows/ci.yml` test job to install the `research` group (`poetry install --with research`) and run `playwright install chromium --with-deps` before pytest. Cache the Playwright browser binary keyed on `playwright` version from `poetry.lock`.
- [x] 1.4 Create `apps/api/src/iguanatrader/contexts/research/scraping/__init__.py` (package marker; re-exports `ScrapeLadder`, `ScrapeBlockedError`, `RateLimitedError`, `is_robots_allowed`, `next_user_agent` for adapter consumption).
- [x] 1.5 Create `apps/api/src/iguanatrader/contexts/research/scraping/errors.py` with three error classes extending `IguanaError`: `ScrapeBlockedError(IguanaError)` (`default_status=503`, `type_uri="urn:iguanatrader:error:scrape-blocked"`, kw fields `tier_attempted`, `block_reason ∈ {challenge_page, http_403, captcha_required, dns_failure}`, `tier_capped: bool = False`); `RateLimitedError(IguanaError)` (`default_status=429`, `type_uri="urn:iguanatrader:error:rate-limited"`, kw `retry_after_seconds`); `SourceUnavailableError(IguanaError)` (`default_status=502`, `type_uri="urn:iguanatrader:error:source-unavailable"`, kw `source_id`, `cause`). Document inline why slice-local rather than `shared/errors.py` (cross-slice anti-collision; T2/K1 also extend in their worktrees).
- [x] 1.6 Create `apps/api/src/iguanatrader/contexts/research/scraping/user_agent.py` with constant `_UA_POOL: tuple[str, ...]` of 5 UA strings each starting with `f"iguanatrader/{__version__} (+arturo6ramirez@gmail.com)"`. Function `next_user_agent() -> str` round-robins via a module-level `itertools.cycle`. Read `__version__` from `iguanatrader.__about__`.
- [x] 1.7 Create `apps/api/src/iguanatrader/contexts/research/scraping/robots_check.py`. Function `is_robots_allowed(url: str, user_agent: str) -> bool` using `urllib.robotparser.RobotFileParser` with a 24h TTL `cachetools.TTLCache(maxsize=512, ttl=86400)` keyed on netloc. Cache miss → fetch `https://<netloc>/robots.txt` via `httpx.get(timeout=10)`; on fetch failure (404, timeout, malformed): emit structlog `research.scraping.robots_unavailable` with `netloc` + `reason`; return `True` (fail-open, FR79 documented choice). Add `cachetools>=5.3` to `pyproject.toml` if not already present (it is — slice 1).
- [x] 1.8 Create `apps/api/src/iguanatrader/contexts/research/scraping/tier1_webfetch.py`. Function `fetch_tier1(url: str, *, ua: str, timeout: float = 30.0) -> tier_response_t` using `httpx.AsyncClient` (HTTP/2). Detect anti-bot signatures: HTTP 403 → raise `ScrapeBlockedError(tier_attempted=1, block_reason="http_403")`; body first 4KB matches one of the regex patterns in `_CHALLENGE_PAGE_PATTERNS = (r"<title>Just a moment\.\.\.", r"cf-challenge", r"_pxhc=", r"<title>Access denied", r"perimeterx-bm-flag")` → raise `ScrapeBlockedError(tier_attempted=1, block_reason="challenge_page")`. HTTP 429 / 503 with `Retry-After` → raise `RateLimitedError(retry_after_seconds=int(retry_after))`. Other 4xx/5xx → raise `SourceUnavailableError`.
- [x] 1.9 Create `apps/api/src/iguanatrader/contexts/research/scraping/tier2_playwright.py`. Function `fetch_tier2(url: str, *, ua: str, timeout: float = 60.0) -> str` launching a Chromium browser per call (cold), `page.goto(url, wait_until="networkidle", timeout=timeout*1000)`, `page.content()`. Same anti-bot signature detection as Tier 1 — page content first 4KB matched against `_CHALLENGE_PAGE_PATTERNS`; persistent CAPTCHA prompt detected via `page.locator("iframe[src*='hcaptcha'], iframe[src*='recaptcha'], [id*='captcha']").count() > 0` → raise `ScrapeBlockedError(tier_attempted=2, block_reason="captcha_required")`. Browser close in `finally`.
- [x] 1.10 Create `apps/api/src/iguanatrader/contexts/research/scraping/tier3_camoufox.py`. Function `fetch_tier3(url: str, *, ua: str | None = None, timeout: float = 60.0) -> str` invoking the Camoufox MCP server via subprocess (uses the existing project MCP wiring; reads server name from `mcp-servers.project.yaml`). Same anti-bot detection; on persistent CAPTCHA → raise `ScrapeBlockedError(tier_attempted=3, block_reason="captcha_required")`.
- [x] 1.11 Create `apps/api/src/iguanatrader/contexts/research/scraping/tier4_captcha.py`. Function `fetch_tier4(url: str, *, ua: str | None = None, timeout: float = 90.0, tenant_id: UUID) -> str` checking `os.environ.get("IGUANA_2CAPTCHA_API_KEY")` first — missing → raise `ScrapeBlockedError(tier_attempted=4, block_reason="captcha_required", tier_capped=True, cause="2CAPTCHA_API_KEY missing")`. Per-tenant daily Tier-4 budget check via `research_sources.metadata.scrape_tier4_daily_usd_max` (default 0.50) read from DB; if cap hit → same error with `tier_capped=True`. Otherwise: invoke `2captcha-python` to solve, re-submit via Tier 3's Camoufox session with the solved token. Emit `api_cost_events` row (slice O1) with `cost_usd=0.003` + `provider='2captcha'`.
- [x] 1.12 Create `apps/api/src/iguanatrader/contexts/research/scraping/ladder.py` with `class ScrapeLadder` exposing `async def fetch(url, *, default_tier, allowed_tiers=(1,2,3,4), tenant_max=3, requires_robots_check=False, tenant_id) -> str`. Algorithm: (a) if `requires_robots_check` and not `is_robots_allowed(url, ua)` → raise `ScrapeBlockedError(block_reason="robots_disallow")`; (b) attempt `default_tier`; (c) on `ScrapeBlockedError` with non-rate-limit reason → escalate to next tier in `allowed_tiers ∩ [next_tier .. min(4, tenant_max)]`; (d) on `RateLimitedError` → `await asyncio.sleep(retry_after); retry same tier max 3 times` then escalate; (e) Tier-4 short-circuit: if `tenant_max < 4` and ladder reaches tier 4 → raise `ScrapeBlockedError(tier_capped=True)`; (f) on success return body. Emit structlog `research.scraping.tier_attempted` at every tier with `url`, `tier`, `outcome`.
- [x] 1.13 Migration `0004_research_sources_tier_b_c.py` with `revision="0004"`, `down_revision="0003"`. `upgrade()` inserts 10 rows into `research_sources` covering source_ids `finnhub`, `gdelt`, `openfda`, `openinsider`, `finviz_scrape`, `wgi_world_bank`, `vdem`, `ibkr_bars`, `yahoo_bars_fallback`, `yfinance_sustainability` — each with `tier ∈ {1,2}`, `pit_class ∈ {'B','C'}` per design D1 + `metadata` JSONB containing `rate_limit_config` (per-source caps), `dedupe_strategy` (per D6), and `scrape_tier4_daily_usd_max=0.50`. `downgrade()` deletes by id. Reversibility verified by inspection.

## 2. Tier-1 webfetch adapters (Finnhub, GDELT, OpenFDA)

- [x] 2.1 Create `apps/api/src/iguanatrader/contexts/research/sources/__init__.py` (package marker; re-export public adapter classes once they land).
- [x] 2.2 Create `apps/api/src/iguanatrader/contexts/research/sources/finnhub.py` with `class FinnhubSource(SourcePort)`. `fetch(symbol, since)` polls three endpoints in order: `/news?symbol=...&from=...&to=...` → news headlines + sentiment; `/calendar/earnings?symbol=...&from=...&to=...` → upcoming earnings; `/stock/recommendation?symbol=...` → consensus ratings. Each result mapped to `ResearchFactDraft` with `source_id="finnhub"`, `fact_kind` ∈ {`news.headline`, `sentiment.score`, `catalyst.earnings_date`, `analyst.rating`}, full provenance (`source_url=full URL with params`, `retrieval_method="api"`, `retrieved_at=utc_now()`), `effective_from = published_at`, `recorded_from = retrieved_at`. Token bucket initialised at 60 req/min; backoff `[3,6,12,24,48]` on 429. Idempotency key `news.uuid` for headlines. `fetch()` is a generator yielding drafts; caller pipes through `repo.insert_fact()`.
- [x] 2.3 Create `apps/api/src/iguanatrader/contexts/research/sources/gdelt.py` with `class GDELTSource(SourcePort)`. Two paths inside one adapter: (a) DOC 2.0 REST polling → `https://api.gdeltproject.org/api/v2/doc/doc?...` for headlines (1 req/3s rate limit); (b) BigQuery historical events → `gdelt-bq.gdeltv2.events` partitioned by `_PARTITIONTIME` filtered by `Actor1Code = :ticker_country OR Actor2Code = :ticker_country` for PESTEL backfill. Pre-execute `--dry_run` BigQuery to estimate bytes; abort if estimated bytes + month-to-date > 90 GB. Map each row to draft with `source_id="gdelt"`, `fact_kind ∈ {news.headline, pestel.event}`. Idempotency key `gkg_id` / `globaleventid`.
- [x] 2.4 Create `apps/api/src/iguanatrader/contexts/research/sources/openfda.py` with `class OpenFDASource(SourcePort)`. Polls `https://api.fda.gov/drug/event.json?search=...`, `/drug/label.json`, `/drug/enforcement.json` for symbols whose company has FDA-regulated products (filter via SymbolUniverse metadata; default skip when no match). Map approvals + recalls to `ResearchFactDraft` with `fact_kind="catalyst.fda_approval"` or `catalyst.fda_recall` + the structured payload to `value_jsonb`. Also write `corporate_events` rows with `event_kind="fda_approval"` via the repository's existing helper (R1) — adapter imports `repo.insert_corporate_event(...)` (added stub helper here if not present in R1; if absent → defer to repository extension subtask 2.4.1).
- [x] 2.4.1 If R1's `ResearchRepository` does NOT expose `insert_corporate_event`, add it here (slice-local extension): same provenance enforcement as `insert_fact`; persists to `corporate_events` table; emits structlog `research.corporate_event.ingested`. Document inline why this addition is R3-scoped (corporate_events writes were not exercised in R1 because R1 had no adapters).
- [x] 2.5 Unit test `apps/api/tests/unit/contexts/research/test_sources_finnhub.py`: mock `httpx.AsyncClient.get` to return canned Finnhub responses (3 fixtures: news / earnings / recommendations); assert each draft carries correct `source_id`, `fact_kind`, `pit_class` resolved via `research_sources` lookup, `source_url`, `retrieval_method="api"`. Assert idempotency: second call with same `since` skips already-ingested news (uuid match).
- [x] 2.6 Unit test `apps/api/tests/unit/contexts/research/test_sources_gdelt.py`: mock both DOC API + BigQuery client; assert dry_run estimate enforcement (mock estimate of 100 GB → adapter aborts with structlog `research.gdelt.quota_alert`); assert rate-limit budget tracking updates `research_sources.metadata.bigquery_bytes_scanned_mtd`.
- [x] 2.7 Unit test `apps/api/tests/unit/contexts/research/test_sources_openfda.py`: mock httpx; one approval response + one recall; assert both `research_facts` AND `corporate_events` rows materialise with linked `source_id`.

## 3. Tier-2 Playwright adapters (OpenInsider, Finviz)

- [x] 3.1 Create `apps/api/src/iguanatrader/contexts/research/sources/openinsider.py` with `class OpenInsiderSource(SourcePort)`. `fetch(symbol, since)` calls `ScrapeLadder.fetch(url=f"http://openinsider.com/screener?s={symbol}&...", default_tier=2, allowed_tiers=(1,2,3,4), tenant_max=tenant_scrape_tier_max, requires_robots_check=True, tenant_id=...)`. Parse the resulting HTML with BS4 + multi-fallback selectors (XPath + CSS class + text-content match cascade per design risk mitigation). Each insider transaction row → `ResearchFactDraft` with `source_id="openinsider"`, `fact_kind="insider.transaction"`, `value_jsonb` carrying `{filer_cik, filer_name, transaction_date, transaction_type, shares, price}`. Idempotency key SHA-1 of `(filer_cik, transaction_date, transaction_type, shares)` stored in `metadata.dedupe_key`. Emit structlog `research.openinsider.parse_fallback_used` with `selector_index` when a fallback selector fires.
- [x] 3.2 Create `apps/api/src/iguanatrader/contexts/research/sources/finviz_scrape.py` with `class FinvizScrapeSource(SourcePort)`. Polls `https://finviz.com/screener.ashx?...&v=152` for analyst-rating screener; on per-symbol pages (`https://finviz.com/quote.ashx?t=<symbol>`) extracts current analyst price targets. Routed through `ScrapeLadder.fetch(default_tier=2, ...)`. Map ratings → `analyst_ratings` table rows + `research_facts` with `fact_kind="analyst.rating"`. Cache: 30-min stale-while-revalidate per (symbol, retrieved_at::date) — second call within 30 min returns cached drafts without re-fetching. Circuit breaker: 5 consecutive Cloudflare 403s in 1h → `enabled=false` for 24h.
- [x] 3.3 Unit test `apps/api/tests/unit/contexts/research/test_sources_openinsider.py`: mock `ScrapeLadder.fetch` to return canned HTML fixtures (3 variants: pristine layout, redesigned layout exercising fallback selector #2, malformed HTML exercising fallback #3); assert correct row count + dedupe_key SHA-1 + fallback-used structlog event when applicable.
- [x] 3.4 Unit test `apps/api/tests/unit/contexts/research/test_sources_finviz_scrape.py`: mock ladder; assert (a) cache hit short-circuits second call within 30min; (b) circuit breaker disables source after 5 consecutive 403s; (c) ratings persisted to BOTH `analyst_ratings` table AND `research_facts` with provenance.

## 4. Tier-1 macro / governance adapters (WGI, V-Dem)

- [x] 4.1 Create `apps/api/src/iguanatrader/contexts/research/sources/wgi_world_bank.py` with `class WGIWorldBankSource(SourcePort)`. Polls `https://api.worldbank.org/v2/country/{code}/indicator/{indicator}?format=json&date=2014:2025` for the 6 WGI indicators (`VA.EST` voice/accountability, `PV.EST` political stability, `GE.EST` government effectiveness, `RQ.EST` regulatory quality, `RL.EST` rule of law, `CC.EST` control of corruption). Map each data point to `ResearchFactDraft` with `source_id="wgi_world_bank"`, `fact_kind="pestel.governance"`, `value_numeric=score`, `effective_from=year_start`, `recorded_from=retrieved_at`, `metadata={"indicator": indicator, "country": code}`. Idempotency `(country, indicator, year)`.
- [x] 4.2 Create `apps/api/src/iguanatrader/contexts/research/sources/vdem.py` with `class VDemSource(SourcePort)`. On first call: download V-Dem v14 dataset CSV bulk file from canonical URL, verify sha256 against published checksum, gzip + cache under `data/research_cache/vdem/v14/<sha256>.csv.gz`. On subsequent calls: read cached file. For each `(country, year, indicator_code)` triple → `ResearchFactDraft` with `source_id="vdem"`, `fact_kind="pestel.democracy_index"`, `pit_class='C'` (bootstrap one-shot per design D7). Filter for the indicators relevant to the watchlist's country mappings; skip the rest.
- [x] 4.3 Unit test `apps/api/tests/unit/contexts/research/test_sources_wgi_world_bank.py`: mock httpx; assert 6 indicators × N years yields the expected number of facts; assert URL contains the documented format.
- [x] 4.4 Unit test `apps/api/tests/unit/contexts/research/test_sources_vdem.py`: mock the bulk-download URL with a small fixture CSV (10 rows); assert sha256 verification; assert second call hits cache (no re-download).

## 5. Bars adapters (IBKR + Yahoo fallback)

- [x] 5.1 Create `apps/api/src/iguanatrader/contexts/research/sources/ibkr_bars.py` with `class IBKRBarsSource(SourcePort, HeartbeatMixin)`. Inherits `HeartbeatMixin` from slice 2 for connection liveness. `fetch(symbol, since)` connects to IB TWS/Gateway via `ib_async`, calls `reqHistoricalData(contract, endDateTime="", durationStr=..., barSizeSetting="1 day"/"1 hour"/etc., whatToShow="TRADES", useRTH=True)`. HMDS pacing: token bucket capacity 60 / refill 1 per 10s; await before each call. On disconnect: backoff `[3,6,12,24,48]`; on reconnect, re-sub the pending request. Each bar → `ResearchFactDraft` with `source_id="ibkr_bars"`, `fact_kind="market.bar"`, `value_jsonb={"o":..,"h":..,"l":..,"c":..,"v":..,"timeframe":...}`, `effective_from=bar_start`, `recorded_from=retrieved_at`. Idempotency `(symbol, timeframe, bar_start)`. Filesystem payload (raw response cached) goes through R1's `with_payload()` if response > 16KB.
- [x] 5.2 Create `apps/api/src/iguanatrader/contexts/research/sources/yahoo_bars_fallback.py` with `class YahooBarsFallbackSource(SourcePort)`. Imports a shared `_yahoo_client.py` with one rate-budget bucket (2000 req/hour) used by BOTH `yahoo_bars_fallback` AND `yfinance_sustainability` (per design D1 last-row note). `fetch(symbol, since)` calls `yf.Ticker(symbol).history(start=since, end=now, interval=...)`. Wrap in `try/except (KeyError, AttributeError, json.JSONDecodeError)` → emit structlog `research.yahoo.shape_regression` + raise `SourceUnavailableError`. Each bar → `ResearchFactDraft` with `source_id="yahoo_bars_fallback"`, same `fact_kind="market.bar"` shape as IBKR for R5's feature_provider compatibility.
- [x] 5.3 Create `apps/api/src/iguanatrader/contexts/research/scraping/_yahoo_client.py` (slice-local — NOT a public scraping primitive). Module-level token bucket `_YAHOO_BUDGET = TokenBucket(capacity=2000, refill_per_hour=2000)`; functions `await acquire()` + `release()` used by both Yahoo adapters.
- [x] 5.4 Unit test `apps/api/tests/unit/contexts/research/test_sources_ibkr_bars.py`: mock `ib_async.IB.reqHistoricalDataAsync` to return canned bar lists; assert 60 calls in 10min budget enforced (61st awaits); assert HeartbeatMixin invoked on disconnect; assert pacing-violation cooldown 600s when IBKR rejects.
- [x] 5.5 Unit test `apps/api/tests/unit/contexts/research/test_sources_yahoo_bars_fallback.py`: mock `yfinance.Ticker.history`; (a) happy path yields drafts; (b) `KeyError` from yfinance shape regression triggers `SourceUnavailableError`; (c) shared budget — call `yfinance_sustainability` adapter in same test ensures budget decremented across both.

## 6. ESG adapter + CI assertion (FR75 enforcement)

- [x] 6.1 Create `apps/api/src/iguanatrader/contexts/research/sources/yfinance_sustainability.py` with `class YFinanceSustainabilitySource(SourcePort)`. Uses shared `_yahoo_client` budget. `fetch(symbol, since)` calls `yf.Ticker(symbol).sustainability` (returns DataFrame or None). Skip silently if None. For each non-NaN cell → `ResearchFactDraft` with `source_id="yfinance_sustainability"`, `fact_kind="esg.aggregate"`, `value_numeric=score`, `unit=score_type` (e.g. `peerEsgScorePerformance`), `metadata={"is_esg_aggregate": true, "single_source_caveat": "yfinance.sustainability — Sustainalytics-via-Yahoo; not independently verified; may regress on yfinance API changes"}`. 24h cache per symbol.
- [x] 6.2 Create `apps/api/tests/unit/test_no_esg_in_backtest.py` — repo-wide CI grep test. Walks `apps/api/src/iguanatrader/contexts/trading/` recursively; opens each `.py` file; uses `tokenize` to strip comments + docstrings; matches the regex `(esg\.aggregate|is_esg_aggregate|fact_kind\s*==\s*['"]esg|esg_score)` against the stripped source. Any match → `pytest.fail(f"FR75 violation: ESG reference found in backtest feature builder: {file}:{line}\nText: {match}")`. Allow file-level pragma escape: `# noqa: esg-ban-allow-context: <reason>` skips the file with a warning emitted via `warnings.warn`.
- [x] 6.3 Unit test `apps/api/tests/unit/contexts/research/test_sources_yfinance_sustainability.py`: mock yfinance; assert metadata flag set + 24h cache; assert `pit_class='B'` resolved.
- [x] 6.4 Self-test the CI assertion: write a temporary fixture file under `apps/api/src/iguanatrader/contexts/trading/_test_esg_violation.py` containing `if fact.fact_kind == "esg.aggregate": ...`; run `test_no_esg_in_backtest` and assert it fails; remove the fixture file. (Or: parametrise the test with an in-memory fixture instead of a real file — preferred to avoid accidental commit.)

## 7. Integration tests + scrape-ladder unit test

- [x] 7.1 Create `apps/api/tests/integration/test_news_ingestion.py` using the slice-3 in-memory SQLite session fixture + R1's `ResearchRepository`. Round-trip: (a) seed `research_sources` rows from migration `0004` (apply migration via `Base.metadata.create_all` + manual seed insert); (b) instantiate `GDELTSource`, mock its HTTP client to yield 3 headline drafts; pipe through `repo.insert_fact()`; assert 3 rows in `research_facts` with correct provenance + `pit_class='B'` resolved via `research_sources` JOIN; (c) repeat for `FinnhubSource` with 2 news drafts; (d) bitemporal query `repo.as_of(symbol, at=between_two_polls)` returns only the GDELT facts (recorded_from semantics).
- [x] 7.2 Create `apps/api/tests/unit/contexts/research/test_scrape_ladder.py`. Tests: (a) Tier 1 success → no escalation, returns body; (b) Tier 1 raises `ScrapeBlockedError` → ladder calls Tier 2; Tier 2 success → returns; (c) Tier 1 raises `RateLimitedError(retry_after_seconds=2)` → ladder sleeps + retries Tier 1 (max 3 retries) + does NOT escalate; (d) Tier 2 raises `ScrapeBlockedError(captcha_required)` + `tenant_max=3` → ladder calls Tier 3; Tier 3 success → returns; (e) Tier 3 raises captcha + `tenant_max=3` → ladder raises `ScrapeBlockedError(tier_capped=True)` (no Tier 4 attempted); (f) Tier 3 raises captcha + `tenant_max=4` + 2Captcha key set → ladder calls Tier 4; Tier 4 mock-solves → returns; structlog `api_cost_events` recorded; (g) `is_robots_allowed` returns False → ladder raises `ScrapeBlockedError(block_reason="robots_disallow")` before any tier attempt.
- [x] 7.3 Add to `test_scrape_ladder.py`: politeness tests — (h) `next_user_agent` returns 5 distinct UAs in 5 calls then cycles; each UA contains `iguanatrader/<version>` + `+arturo6ramirez@gmail.com`; (i) `is_robots_allowed("https://example.com/disallowed")` consults RobotFileParser correctly; (j) `is_robots_allowed` 24h cache hit avoids re-fetching robots.txt.

## 8. Documentation + verification

- [x] 8.1 Append to `docs/gotchas.md` (R3 entries — number sequentially after the highest existing): GDELT BigQuery monthly budget tracker + dry_run pre-check; Finnhub free-tier 60 req/min token bucket; OpenFDA soft-rate latency monitor; OpenInsider DOM brittleness + multi-fallback selector cascade; Finviz 30-min cache + Cloudflare circuit breaker; yfinance undocumented internal API regression handling; IBKR HMDS pacing 60/10min cooldown; Yahoo shared rate budget across two adapters; 2Captcha tier-4 daily $0.50 cap + per-tenant feature flag; ESG single-source caveat + CI grep enforcement.
- [x] 8.2 Append to `apps/api/README.md` "Research adapters (R3)" subsection: bullet list of 9 source_ids with their `pit_class` + `tier` + idempotency keys; quick-reference for invoking `ScrapeLadder.fetch()`; pointer to `test_no_esg_in_backtest.py` for FR75 enforcement; diagram (ASCII) of the 4-tier ladder with escalation arrows.
- [x] 8.3 Update ADR-017 status from `proposed` to `accepted` (R3 is the slice that lands the ladder per ADR cross-reference). Replace the "Pending" full-content section with a back-reference to this slice's `design.md` D2 + D3.
- [x] 8.4 `mypy --strict apps/api/src/iguanatrader/contexts/research/sources/ apps/api/src/iguanatrader/contexts/research/scraping/ apps/api/src/iguanatrader/migrations/versions/0004_research_sources_tier_b_c.py` — clean.
- [x] 8.5 `pytest apps/api/tests/unit/contexts/research/test_sources_*.py apps/api/tests/unit/contexts/research/test_scrape_ladder.py apps/api/tests/integration/test_news_ingestion.py apps/api/tests/unit/test_no_esg_in_backtest.py -v` — all green.
- [x] 8.6 Coverage on the listed modules ≥ 80% per NFR-M1 (`pytest --cov=apps/api/src/iguanatrader/contexts/research/sources --cov=apps/api/src/iguanatrader/contexts/research/scraping --cov-fail-under=80`).
- [x] 8.7 `pre-commit run --from-ref origin/main --to-ref HEAD` — gitleaks (no API keys committed; fixtures use placeholder strings), ruff, black, mypy, license-boundary-check.
- [x] 8.8 `alembic upgrade head` clean against fresh SQLite + reversibility check (`alembic upgrade head && alembic downgrade -1 && alembic upgrade head`) — all 10 catalogue rows materialise + drop + re-materialise.
- [x] 8.9 Confirm no shared infra files were edited via `git diff --name-only origin/main...HEAD`. Allowed paths: `apps/api/src/iguanatrader/contexts/research/{sources,scraping}/`, `apps/api/src/iguanatrader/migrations/versions/0004_*.py`, `apps/api/tests/{unit,integration}/contexts/research/test_*`, `apps/api/tests/unit/test_no_esg_in_backtest.py`, `apps/api/README.md`, `docs/gotchas.md`, `docs/adr/ADR-017-*.md` (status promotion only), `pyproject.toml` + `poetry.lock`, `.github/workflows/ci.yml`, `openspec/changes/research-news-catalysts-adapters/*`. Verify NONE of `app.py`, `routes/__init__.py`, `sse/__init__.py`, `cli/main.py`, `migrations/env.py`, `persistence/*.py`, `shared/errors.py`, `contexts/research/{models,ports,repository,events}.py` (R1-owned) are touched. Anti-collision compliance: full.
- [x] 8.10 `python .ai-playbook/scripts/openspec_validate.py research-news-catalysts-adapters` — proposal + design + tasks + spec validate clean.
