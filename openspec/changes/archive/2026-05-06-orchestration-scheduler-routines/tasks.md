## 1. Setup + APScheduler scaffold + ET timezone config

- [x] 1.1 Add runtime deps to root `pyproject.toml`: `apscheduler = ">=3.10,<4.0"`, `langgraph = ">=0.2,<0.3"`, `reportlab = ">=4.0,<5.0"`. Dev dep: `freezegun = ">=1.5,<2.0"`. Lock via `regenerate-lock.yml` workflow_dispatch + commit (per gotcha #18 the local poetry path is fragile). Pull regenerated lock back to slice branch before continuing.
- [x] 1.2 Verify slice-5 dynamic-discovery contract still loads `sse/<name>.py` modules without `app.py` edits (re-run existing `test_dynamic_discovery.py::test_new_sse_module_registered_without_app_py_edit` smoke; CI-only step).
- [x] 1.3 Create directory `apps/api/src/iguanatrader/contexts/orchestration/` with `__init__.py` (package marker; docstring documents bounded-context boundary — orchestration owns: scheduler, routine workflows, alert filter, weekly PDF, routine_runs + alert_events tables).
- [x] 1.4 Create `apps/api/src/iguanatrader/contexts/orchestration/scheduler.py` per design D1+D2: `create_scheduler(engine) -> AsyncIOScheduler` with `SQLAlchemyJobStore`, `AsyncIOExecutor`, `coalesce=True, max_instances=1, misfire_grace_time=300`, `timezone=ZoneInfo("America/New_York")`. Exports `register_routine_jobs(scheduler, service)` that registers 4 `CronTrigger`s (premarket/midday/postmarket/weekly_review) with the schedules from D2.
- [x] 1.5 Create `apps/api/src/iguanatrader/contexts/orchestration/errors.py` with slice-local `IguanaError` subclasses: `RoutineTimeoutError(default_status=500, type_uri="urn:iguanatrader:error:routine-timeout")`, `RoutineBudgetExceededError(default_status=402, type_uri="urn:iguanatrader:error:routine-budget-exceeded")`, `DuplicateRoutineTriggerError(default_status=409, type_uri="urn:iguanatrader:error:duplicate-routine-trigger")`, `AlertChannelUnavailableError(default_status=503, type_uri="urn:iguanatrader:error:alert-channel-unavailable")`. Inherits `IguanaError`; global RFC 7807 handler renders automatically.

## 2. Models + migration

- [x] 2.1 Create `apps/api/src/iguanatrader/contexts/orchestration/models.py` with SQLAlchemy mappings: `RoutineRun` (`id`, `tenant_id`, `routine_name`, `scheduled_at`, `started_at`, `ended_at`, `status`, `duration_ms`, `cost_usd`, `digest_payload JSONB`, `error_message`), `AlertEvent` (`id`, `tenant_id`, `event_name`, `tier`, `routing_decision JSONB`, `payload JSONB`, `correlation_id`, `created_at`, `source_event_id`, `source_event_name`). `RoutineRun.__tablename_is_append_only__ = "running_to_terminal"` (D5 special string); `AlertEvent.__tablename_is_append_only__ = True`.
- [x] 2.2 Update `apps/api/src/iguanatrader/persistence/append_only_listener.py` to interpret the `"running_to_terminal"` special string: allow UPDATE only when `before.status == 'running' AND after.status IN ('success','timeout','error','skipped_budget','skipped_duplicate')`; raise `AppendOnlyViolationError` for any other UPDATE pattern. Per design D5. Document inline why this exception exists.
- [x] 2.3 Create `apps/api/src/iguanatrader/contexts/orchestration/repository.py` with `RoutineRunRepository` (methods: `insert_running(routine_name, scheduled_at, tenant_id) -> UUID`, `mark_success(run_id, duration_ms, cost_usd, digest_payload)`, `mark_timeout(run_id, duration_ms, last_node)`, `mark_error(run_id, error_message)`, `mark_skipped(run_id, reason, fallback_digest)`, `clean_orphan_runs(threshold_minutes=10)`, `query_recent(routine_name, limit)`) and `AlertEventRepository` (methods: `insert(event)`, `query_by_tenant_and_tier(tenant_id, tiers, since)`, `stream_for_sse(tenant_id, tiers)`). All repositories extend `BaseRepository` (slice 2).
- [x] 2.4 Create `apps/api/src/iguanatrader/migrations/versions/0007_orchestration_tables.py` (Alembic, `down_revision='0006_observability_tables'` per merge plan). Tables: `routine_runs` (per-tenant, "mutable-then-frozen" — CHECK constraint on `status` transitions enforces D5 contract; UNIQUE(`routine_name`, `scheduled_at`) for D9 idempotency; indexes `ix_routine_runs_tenant_id_scheduled_at`, `ix_routine_runs_routine_name_status`), `alert_events` (per-tenant, append-only; indexes `ix_alert_events_tenant_id_tier_created_at`, `ix_alert_events_event_name_created_at`, `ix_alert_events_correlation_id`). Naming convention follows slice-3 contract. Migration docstring explicitly notes APScheduler's `apscheduler_jobs` table is auto-created out-of-band and MUST NOT be in this migration.
- [x] 2.5 Create `apps/api/src/iguanatrader/contexts/orchestration/events.py` declaring MessageBus event names + dataclass payloads: `orchestration.routine.started`, `orchestration.routine.success`, `orchestration.routine.timeout`, `orchestration.routine.skipped_budget`, `orchestration.routine.duplicate_trigger`, `orchestration.<routine>.digest_published` (one per routine), `orchestration.weekly_review.pdf_ready`, `orchestration.alert.tier_classified`. Each event has `EVENT_NAME` ClassVar.

## 3. Alert filter + tier-1 rules engine

- [x] 3.1 Create `apps/api/src/iguanatrader/contexts/orchestration/tier1_alerts.py` per design D4: hardcoded `TIER_1_RULES: dict[str, AlertRule]` table covering minimum: `risk.kill_switch.tripped`, `trading.ibkr.disconnected_90s`, `research.fda.approval_on_watchlist`, `research.insider.buy_pct` (with `predicate=lambda payload: payload.get("pct", 0) >= 10`), `observability.budget.block_100`, `orchestration.routine.timeout`, `approval.proposal.timed_out` (TIER_2 actually; documented inline). Each rule has `event_name`, `tier`, `predicate: Callable[[dict], bool]`, `routing_decision_template`. Documented as data-not-algorithm (table edits = no behaviour change, per O1 D2 precedent).
- [x] 3.2 Create `apps/api/src/iguanatrader/contexts/orchestration/alert_filter.py`: `AlertFilter` class with `subscribe_all(message_bus)` (registers callbacks for all rule event_names at startup), `classify_event(event_name, payload) -> AlertTier` (rules-based, <1ms — NO LLM call), `_handle_tier_1(event)` (persists `alert_events`, publishes `MessageBus("approval.alert.tier_1", ...)`), `_handle_tier_2(event)` (persists `alert_events`, queues for next routine via in-memory `deque[AlertEvent]` per tenant), `_handle_tier_3(event)` (persists to `audit_log` only). Per-tenant tier-2 queue exposed via `drain_tier2_queue(tenant_id) -> list[AlertEvent]` consumed by `nodes/<routine>.py::filter_alerts`.
- [x] 3.3 Create `apps/api/tests/unit/contexts/orchestration/test_alert_filter.py`: cases (a) tier-1 rule `risk.kill_switch.tripped` → `_handle_tier_1` invoked → `alert_events` row inserted with `tier=1` + MessageBus publish to `approval.alert.tier_1`; (b) tier-1 rule `research.insider.buy_pct` with `pct=10` → tier-1; with `pct=5` → tier-3 (no rule match falls through); (c) tier-2 event accumulates into per-tenant deque; (d) `drain_tier2_queue` returns + clears queue; (e) tier-3 event persists to `audit_log` only (no MessageBus publish, no queue add).
- [x] 3.4 Create `apps/api/tests/unit/contexts/orchestration/test_tier1_rules.py`: parametrised over the full TIER_1_RULES table — for each rule, assert `classify_event(event_name, sample_payload) == TIER_1`. Negative case: unknown event_name → defaults to TIER_3 (audit only).

## 4. Premarket routine LangGraph workflow

- [x] 4.1 Create `apps/api/src/iguanatrader/contexts/orchestration/nodes/__init__.py` + `nodes/_common.py` per design D3: `RoutineState` Pydantic model (`tenant_id`, `routine_run_id`, `scheduled_at`, `routine_name`, `facts: list[Fact]`, `digest: RoutineDigest | None`, `alerts_emitted: list[AlertEvent]`, `errors: list[str]`); `Fact` Pydantic model (`source`, `entity`, `payload`, `effective_from`, `recorded_at`); `RoutineDigest` Pydantic model (`narrative_text`, `bullet_points`, `alert_candidates`, `metadata`); shared helpers `build_fallback_digest(facts) -> RoutineDigest` (D6 deterministic fallback when budget BLOCK_100), `render_prompt(template_path, state) -> str` (Jinja2).
- [x] 4.2 Create `apps/api/src/iguanatrader/contexts/orchestration/nodes/premarket.py`: `build_premarket_graph() -> CompiledGraph` returning a LangGraph `StateGraph[RoutineState]` with 4 nodes (`collect_facts`, `synthesize_brief`, `filter_alerts`, `publish_digest`) wired sequentially + `MemorySaver()` checkpointer. `collect_facts` queries `research_facts` since previous postmarket fire (uses `RoutineRunRepository.query_recent("postmarket", limit=1)` to find boundary), `trade_proposals` overnight, earnings calendar from R2 (FRED + EDGAR), top-5 overnight news from R3 (Finnhub + GDELT). `synthesize_brief` calls `route_llm(TaskClass.ROUTINE_SUMMARY)` + LLM SDK wrapped in `@cost_meter("anthropic", "claude-3-5-haiku")`. `filter_alerts` runs facts + digest.alert_candidates through `AlertFilter.classify_event`. `publish_digest` persists to `routine_runs.digest_payload` + emits MessageBus `orchestration.premarket.digest_published`.
- [x] 4.3 Create `apps/api/src/iguanatrader/contexts/orchestration/prompts/premarket.md`: Jinja2 template with placeholders `{{ overnight_news }}`, `{{ earnings_today }}`, `{{ insider_buys_overnight }}`, `{{ macro_events_today }}`, `{{ open_positions }}`. Body instructs LLM: "Generate a 2-min-read briefing covering: (1) what changed overnight, (2) earnings to watch today, (3) catalysts on watchlist symbols, (4) macro context, (5) recommended posture. Use bullet points; cite fact_ids in `[fact_id]` markers."
- [x] 4.4 Create `apps/api/tests/unit/contexts/orchestration/test_premarket_graph.py`: cases (a) 4-node graph compiles + executes end-to-end against mocked R1/R2/R3 repositories + `_FakeLLMClient` returning canned `LLMResponse`; (b) `collect_facts` correctly bounds window via previous postmarket; (c) `publish_digest` writes `routine_runs.digest_payload` + emits `orchestration.premarket.digest_published`; (d) budget WARN_80 → routing returns haiku; budget BLOCK_100 → fallback digest path + no LLM call.

## 5. Midday routine workflow

- [x] 5.1 Create `apps/api/src/iguanatrader/contexts/orchestration/nodes/midday.py`: same 4-node shape as premarket. `collect_facts` queries open positions from `equity_snapshots` (T1) intraday, `trade_proposals` since premarket fire, `risk_evaluations` since premarket fire, drained tier-2 queue from `AlertFilter.drain_tier2_queue(tenant_id)`. `synthesize_brief` via Haiku. Output focuses on "pulse check" — open positions delta + risk dashboard summary + tier-2 alerts since premarket.
- [x] 5.2 Create `apps/api/src/iguanatrader/contexts/orchestration/prompts/midday.md`: Jinja2 template with placeholders `{{ open_positions_pnl }}`, `{{ proposals_since_premarket }}`, `{{ risk_utilization }}`, `{{ tier_2_alerts }}`. Body: "Provide an intraday pulse: (1) P&L of open positions, (2) any cap utilization warnings, (3) deferred tier-2 events from premarket onwards, (4) recommended action if any."
- [x] 5.3 Create `apps/api/tests/unit/contexts/orchestration/test_midday_graph.py`: cases (a) tier-2 queue drained on graph entry; (b) re-running midday graph in same window does NOT re-emit tier-2 events (queue is empty); (c) digest payload includes risk utilization metrics from K1's `risk_evaluations`.

## 6. Postmarket routine workflow

- [x] 6.1 Create `apps/api/src/iguanatrader/contexts/orchestration/nodes/postmarket.py`: same 4-node shape. `collect_facts` queries today's `fills` (T1), today's closed `trade_proposals`, today's `risk_evaluations`, end-of-day `equity_snapshots`. `synthesize_brief` via Haiku. Output: end-of-day P&L summary + next-day watchlist refresh + lessons-of-the-day candidates (collected for weekly_review).
- [x] 6.2 Create `apps/api/src/iguanatrader/contexts/orchestration/prompts/postmarket.md`: Jinja2 template with placeholders `{{ todays_fills }}`, `{{ todays_pnl }}`, `{{ closed_trades_today }}`, `{{ next_day_watchlist }}`. Body: "End-of-session summary: (1) realised P&L today, (2) trades closed with reasoning excerpts, (3) tomorrow's watchlist refresh, (4) any lessons emerging."
- [x] 6.3 Create `apps/api/tests/unit/contexts/orchestration/test_postmarket_graph.py`: cases (a) full session of fills aggregated correctly; (b) lessons-of-the-day collected into `digest.metadata.lessons_candidates` for later weekly_review consumption; (c) next-day watchlist computed from current open positions + research_facts.

## 7. Weekly review routine + PDF report

- [x] 7.1 Create `apps/api/src/iguanatrader/contexts/orchestration/report_pdf.py` per design D7: `build_weekly_review_pdf(state: RoutineState) -> bytes` using `reportlab.platypus` (SimpleDocTemplate, Paragraph, Table, Spacer, Image). 7 sections per D7. Equity curve: matplotlib renders PNG → embedded via `reportlab.platypus.Image`. Output: PDF bytes ready for upload as Telegram document or saved to filesystem for Hermes file link.
- [x] 7.2 Create `apps/api/src/iguanatrader/contexts/orchestration/nodes/weekly_review.py`: 4-node shape but `synthesize_brief` calls `route_llm(TaskClass.COMPLEX_SYNTHESIS)` → claude-3-opus (week-level reasoning warrants the upgrade per O1 D2). `collect_facts` queries past 7 calendar days of `fills`, `trade_proposals`, `risk_evaluations`, `equity_snapshots`, postmarket lessons_candidates. `publish_digest` calls `report_pdf.build_weekly_review_pdf(state)` + emits MessageBus `orchestration.weekly_review.pdf_ready` with `{tenant_id, pdf_bytes, filename: "weekly-review-{iso_date}.pdf"}`.
- [x] 7.3 Create `apps/api/src/iguanatrader/contexts/orchestration/prompts/weekly_review.md`: longer Jinja2 template covering: weekly P&L summary, per-strategy breakdown, top-3 winners + top-3 losers with reasoning, lessons section (LLM extracts patterns from postmarket lessons_candidates), outlook section (next-week earnings calendar + macro events from R2/R3). Body instructs LLM: "Generate a comprehensive weekly retrospective in Markdown; sections: Executive Summary, Trade-by-Trade Analysis, Strategy Performance, Cost Breakdown, Lessons Learned, Outlook for Next Week."
- [x] 7.4 Create `apps/api/tests/unit/contexts/orchestration/test_report_pdf.py`: cases (a) PDF generation completes <10s for synthetic 50-trade week; (b) PDF is non-empty bytes + valid PDF magic header; (c) all 7 sections render even when individual sections are empty (e.g., 0 trades → Trade Table page renders "No trades this week"); (d) equity curve PNG embeds without error when matplotlib backend is non-interactive (Agg). Skip on missing matplotlib.

## 8. SSE alerts endpoint

- [x] 8.1 Create `apps/api/src/iguanatrader/api/dtos/orchestration.py`: `RoutineRunDTO`, `AlertEventDTO`, `RoutineDigestDTO`, `AlertTierEnum` (StrEnum: `TIER_1`, `TIER_2`, `TIER_3`). Pydantic v2 models; flow into `packages/shared-types` via slice-5 typegen pipeline.
- [x] 8.2 Create `apps/api/src/iguanatrader/api/sse/alerts.py`: exports `router: APIRouter` with `GET /alerts` SSE endpoint. Authenticated via `get_current_user`; tenant-scoped via `tenant_id_var`. Query param `?tier=1,2` filters which tiers to stream (default `1,2`; admin role can pass `1,2,3`). Subscribes to `MessageBus("orchestration.alert.tier_classified", ...)` events + filters by tenant + tier. Slice-5 dynamic discovery picks up the file.
- [x] 8.3 Create `apps/api/tests/integration/test_sse_alerts.py`: cases (a) authenticated client receives tier-1 events for own tenant in real time; (b) tier filter `?tier=1` excludes tier-2/3 events; (c) cross-tenant isolation: client A does NOT receive client B's alerts; (d) unauthenticated request returns 401 RFC 7807.

## 9. Cost meter integration + budget gating

- [x] 9.1 Create `apps/api/src/iguanatrader/contexts/orchestration/service.py` per design D6+D8+D9: `OrchestrationService` class with `start(message_bus, scheduler)` (registers cron jobs + alert filter subscriptions + cleans orphan runs at boot), `run_routine(name, scheduled_at, tenant_id)` (the cron callback — calls `check_budget(tenant_id)` first, branches on OK/WARN_80/BLOCK_100; wraps LangGraph `ainvoke` in `asyncio.wait_for(timeout=300)`; catches `IntegrityError` on duplicate trigger; persists routine_runs lifecycle), `run_routine_now(name, tenant_id)` (manual trigger; D10 — exposed as service method only, no REST endpoint here). Routine LLM calls capture the routine_run_id via a `routine_run_id_var` contextvar so `cost_meter` decorator persists it onto `api_cost_events.routine_run_id` (FK populated for the first time — slice O1 had this column NULL).
- [x] 9.2 Update `apps/api/src/iguanatrader/contexts/observability/cost_meter.py::_persist_cost_event` to read `routine_run_id_var` (new contextvar in `iguanatrader.shared.contextvars`) and populate `ApiCostEvent.routine_run_id`. **Cross-context edit allowed**: the column was added in O1 explicitly anticipating this slice (per O1 spec scenario "`routine_run_id` (NULL until slice O2 wires it)"). Documented as designed cross-slice continuation, not boundary violation.
- [x] 9.3 Create `apps/api/tests/integration/test_routine_cost_attribution.py`: cases (a) routine LLM call inside `synthesize_brief` populates `api_cost_events.routine_run_id` with the current `RoutineRun.id`; (b) cost rolled up into `routine_runs.cost_usd` at routine exit via `SUM(cost_usd) FROM api_cost_events WHERE routine_run_id = :id`; (c) BLOCK_100 → no LLM call → `routine_runs.cost_usd = 0`.

## 10. Integration test (mocked clock + LangGraph workflow)

- [x] 10.1 Create `apps/api/tests/integration/test_orchestration.py` per design — comprehensive end-to-end. Uses `freezegun.freeze_time` for deterministic cron firing + APScheduler `executors={"default": AsyncIOExecutor()}` synchronous-mode trick for in-test execution. Cases:
  - (a) cron fires premarket at 06:30 ET on a weekday → `routine_runs` row inserted with `status='success'`, digest published.
  - (b) duplicate cron trigger (manual `service.run_routine_now` colliding with scheduled fire) → IntegrityError → `status='skipped_duplicate'` for the loser; winner completes normally.
  - (c) budget BLOCK_100 → `status='skipped_budget'` + fallback deterministic digest emitted.
  - (d) tier-1 event during routine (e.g., `risk.kill_switch.tripped` published mid-`synthesize_brief`) → AlertFilter publishes `approval.alert.tier_1` within <1s + `alert_events.tier=1` row persisted.
  - (e) routine timeout (LLM SDK fake hangs forever) → `asyncio.wait_for` cancels + `status='timeout'` + tier-1 alert `orchestration.routine.timeout` emitted.
  - (f) weekly_review fires Sunday 18:00 ET → PDF generated + `orchestration.weekly_review.pdf_ready` event emitted with non-empty bytes.
  - (g) bootstrap janitor: insert `routine_runs(status='running', started_at=now-15min)` → on `service.start()`, row updated to `status='timeout'` with `error_message='process_crashed_during_routine'`.
- [x] 10.2 Create `apps/api/tests/unit/contexts/orchestration/test_routine_idempotency.py`: cases (a) UNIQUE constraint on (`routine_name`, `scheduled_at`) rejects duplicate INSERT; (b) `service.run_routine` handles IntegrityError gracefully + writes `skipped_duplicate` row at `scheduled_at + 1ms`; (c) the original (winning) routine completes unaffected.
- [x] 10.3 Create `apps/api/tests/unit/contexts/orchestration/test_et_timezone_dst.py` per design D2: parametrised over 2026-03-08 (spring-forward) + 2026-11-01 (fall-back). For each date, walk freezegun across the day + assert each cron (premarket, midday, postmarket, weekly_review if Sunday) fires exactly once, not zero (skipped) and not twice (re-fired). Specifically asserts cron does NOT fire at the DST-window 02:00-03:00 ET edge.

## 11. Verification

- [x] 11.1 `mypy --strict` on `apps/api/src/iguanatrader/contexts/orchestration/`, `api/sse/alerts.py`, `api/dtos/orchestration.py`, `migrations/versions/0007_orchestration_tables.py`, `persistence/append_only_listener.py` (modified for D5 special string), `contexts/observability/cost_meter.py` (modified for routine_run_id_var per task 9.2). Expect zero errors.
- [x] 11.2 `pytest apps/api/tests/integration/test_orchestration.py apps/api/tests/integration/test_sse_alerts.py apps/api/tests/integration/test_routine_cost_attribution.py apps/api/tests/unit/contexts/orchestration/` — all green.
- [x] 11.3 Coverage gate: `pytest --cov apps/api/src/iguanatrader/contexts/orchestration --cov-fail-under=80`. The integration test surface targets the public API of every orchestration module so coverage on `apps/api/src/iguanatrader/contexts/orchestration/*.py` is expected to exceed 80%.
- [x] 11.4 `pre-commit run --from-ref origin/main --to-ref HEAD` — lint + black + ruff + mypy + secrets-scan + license-boundary clean.
- [x] 11.5 ET timezone smoke test (manual or scripted): boot the app with `freezegun` patched to 2026-03-08T06:25:00 ET; assert that at T+5min (06:30 ET) the premarket cron fires exactly once. Repeat for 2026-11-01 (fall-back). This complements the unit test in task 10.3 with a process-level smoke.
- [x] 11.6 `alembic upgrade head` + `alembic downgrade -1` round-trip: forward migration creates `routine_runs`, `alert_events` with all indexes + UNIQUE; reverse drops them cleanly. APScheduler's `apscheduler_jobs` is left untouched (verified by re-running scheduler boot post-downgrade — it re-creates the table).
- [x] 11.7 Document in `docs/gotchas.md`: gotcha #64 (routine_runs "mutable-then-frozen" exception to append-only — D5), gotcha #65 (APScheduler `apscheduler_jobs` table is auto-created out-of-band of Alembic — must NOT be in our migrations), gotcha #66 (LangGraph memory checkpointer loses state on process restart — routines are idempotent at the run level via UNIQUE constraint), gotcha #67 (tier-2 alert queue is in-memory per-tenant deque; lost on restart but that is acceptable since tier-2 is "next routine digest" not "must deliver").
- [x] 11.8 Update `docs/architecture-decisions.md` with cross-reference section "Slice O2 cross-references (added 2026-05-06)" — APScheduler `SQLAlchemyJobStore` choice + LangGraph workflow per routine + `zoneinfo` DST handling + tier-1 inline-with-publication subscription pattern. No new ADR required (these are tactical decisions); the cross-reference points readers to design D1-D9 in this slice.
- [x] 11.9 Update `apps/api/README.md`: document the routine timeout env vars (`IGUANATRADER_ROUTINE_TIMEOUT_<NAME>_SECONDS`), the manual trigger CLI deferred message ("`iguanatrader ops run-routine <name>` lands in slice T4"), the weekly PDF location convention (`logs/weekly-review-{iso_date}.pdf` if filesystem fallback used), and APScheduler operational notes (single-process MVP, `coalesce=True` collapses missed cron, bootstrap janitor cleans orphan rows).
- [x] 11.10 PR description "AI-reviewer signoff" subsection — to be authored when the PR is opened. Self-review log embedded in commit messages + agent report.
