## 1. Setup + base TierASourceAdapter

- [x] 1.1 Verify R2 prerequisites are merged on main: slice `research-bitemporal-schema` archived (check `openspec/changes/archive/2026-05-06-research-bitemporal-schema/` exists). If missing, halt as `❓ blocked-by-spec`.
- [x] 1.2 Confirm new dev deps `vcrpy` + `pytest-vcr` need adding to `pyproject.toml [tool.poetry.group.dev.dependencies]`; no production deps. If a candidate prod dep emerges (e.g., `edgartools`), document in `proposal.md` Impact + halt for human review per design D1.
- [x] 1.3 Confirm migration number `0004` is the next available slot (check `apps/api/src/iguanatrader/migrations/versions/` — R1 shipped `0003_research_tables.py`; auth shipped `0002_users_role_enum.py`). If a `0004_*.py` already exists from a parallel Wave 3 slice, halt — coordinate slot allocation. Document the actual number used in commit body.
- [x] 1.4 Create `apps/api/src/iguanatrader/contexts/research/sources/__init__.py` (empty package marker; first time the `sources/` directory exists). NO re-exports — concrete adapter classes are imported by their FQN by R5/O2 schedulers.
- [x] 1.5 Create `apps/api/src/iguanatrader/contexts/research/sources/base.py` with abstract `TierASourceAdapter(SourcePort)` exposing: class attributes `source_id: ClassVar[str]`, `rate_limit_per_second: ClassVar[float]`, `default_user_agent: ClassVar[str]`; instance attributes `_client: httpx.AsyncClient` (lazy), `_rate_limiter: TokenBucket` (class-shared per design risk note); abstract `async def fetch(self, symbol: str, since: datetime | None) -> AsyncIterator[ResearchFactDraft]`; concrete `async def _request(self, method, url, **kwargs) -> httpx.Response` wrapping rate-limit + tenacity retry with `exponential_backoff([3,6,12,24,48])` from slice 2 + 4xx-permanent-skip handling per spec; concrete `_make_draft(...)` helper that sets `tier='A'`, `retrieval_method='api'`, `retrieved_at=time.utc_now()`; concrete `async def _emit(self, draft) -> None` calling repository's dedupe wrapper.
- [x] 1.6 Create `apps/api/src/iguanatrader/contexts/research/sources/_dedupe.py` — slice-local helper `async def insert_fact_with_dedupe(repository, draft, dedupe_key)` that does `SELECT 1 FROM research_facts WHERE tenant_id = :tid AND dedupe_key = :key LIMIT 1`, returns existing fact if hit (logs `research.<source>.skipped_duplicate` based on `draft.source_id`), else calls `repository.insert_fact(draft)` after attaching the dedupe_key to the draft. This is the design-D7 default path that does NOT modify R1's `insert_fact` signature; if PR review prefers, the alternative additive parameter approach is also acceptable (document the choice in PR body).
- [x] 1.7 Create `apps/api/src/iguanatrader/contexts/research/errors.py` additions: `SourceUnavailableError(IguanaError, default_status=503, type_uri="urn:iguanatrader:error:source-unavailable")`; `RateLimitedError(IguanaError, default_status=429, type_uri="urn:iguanatrader:error:rate-limited")`; `ConfigError(IguanaError, default_status=500, type_uri="urn:iguanatrader:error:config")`. Re-export from `contexts/research/__init__.py` (additive; no edits to `shared/errors.py` per anti-collision rule).
- [x] 1.8 Create `apps/api/src/iguanatrader/contexts/research/sources/_token_bucket.py` — minimal token-bucket rate limiter with `await acquire()` semantics. Class-level instance per concrete adapter (not per instance) so multiple `SECEdgarSource()` objects in the same process share one bucket. Unit tested in 6.x.
- [x] 1.9 Create `apps/api/src/iguanatrader/migrations/versions/0004_research_dedupe_index.py` (number TBC per task 1.3): `revision="0004"`, `down_revision="0003"` (or whatever R1's actual slot was). `upgrade()`: `op.add_column("research_facts", sa.Column("dedupe_key", sa.Text(), nullable=True))` + `op.create_index("idx_research_facts_dedupe_key", "research_facts", ["tenant_id", "dedupe_key"], unique=True, postgresql_where=sa.text("dedupe_key IS NOT NULL"), sqlite_where=sa.text("dedupe_key IS NOT NULL"))`. `downgrade()` drops index then column. Reversible.

## 2. SEC EDGAR adapter (TDD: tests first per `apps/api/tests/unit/contexts/research/sources/test_sec_edgar.py`)

- [x] 2.1 Create `apps/api/tests/unit/contexts/research/sources/__init__.py` package marker.
- [x] 2.2 Create `apps/api/tests/unit/contexts/research/sources/test_sec_edgar.py` — RED phase: write all assertions before implementation. Tests: (a) `SECEdgarSource()` raises `ConfigError` if `SEC_EDGAR_USER_AGENT` env unset; (b) raises `ConfigError` if UA doesn't match regex `^.+ .+@.+\..+$`; (c) successful init does NOT make any HTTP request; (d) given mocked submissions API response, adapter yields drafts with `source_id="sec_edgar"`, `tier="A"`, `effective_from = filingDate at T00:00:00Z`, `dedupe_key = f"sec_edgar:{accession_number}"`; (e) Form 4 transaction draft uses `transaction_date` not `filingDate` for `effective_from`; (f) 404 on a CIK lookup yields nothing + emits `research.sec_edgar.permanent_skip`; (g) 503 retries 5 times then raises `SourceUnavailableError`; (h) re-running `fetch` after first call inserts zero (idempotent via dedupe).
- [x] 2.3 Create `apps/api/src/iguanatrader/contexts/research/sources/sec_edgar.py` implementing `SECEdgarSource(TierASourceAdapter)` to make 2.2 tests pass. Class attrs: `source_id="sec_edgar"`, `rate_limit_per_second=10.0`. Init: reads `SEC_EDGAR_USER_AGENT` env, validates regex. Methods: `_resolve_cik(symbol)` (cached via `functools.lru_cache` against company-tickers JSON), `_fetch_submissions(cik, since)` (paginated through `files[]` for pre-1000 filings), `_parse_filing_to_draft(filing, cik)`, `_parse_form4_xml(filing, primary_doc_url)` (uses `xml.etree.ElementTree`), `_fetch_xbrl_facts(cik, since)` (only for 10-K/10-Q form types). `fetch(symbol, since)` orchestrates: resolve CIK → fetch submissions → for each filing emit metadata draft + (if 10-K/10-Q) XBRL drafts + (if Form 4) per-transaction drafts.
- [x] 2.4 Create VCR cassette `apps/api/tests/fixtures/vcr/edgar_aapl_form4.yaml` by running the adapter once against live EDGAR with a real UA, then committing the cassette (verify scrubbing per spec requirement). Same for `edgar_aapl_10k.yaml` (a 10-K with XBRL). Document recording procedure in `apps/api/tests/integration/README.md`.
- [x] 2.5 Create `apps/api/tests/integration/test_edgar_ingestion.py` using VCR cassettes + the slice-3 in-memory SQLite fixture: (a) running `SECEdgarSource().fetch("AAPL", since=2024-01-01)` populates research_facts with N rows; (b) running again inserts 0 new rows; (c) every emitted fact has `tier="A"`, `retrieval_method="api"`, populated `effective_from`, populated `dedupe_key`; (d) Form 4 facts have `effective_from = transaction_date`; (e) XBRL facts have `value_numeric IS NOT NULL` and `fact_kind LIKE 'sec_xbrl.%'`.
- [x] 2.6 Verify the conftest's `vcr_config` filters `api_key`, `registrationkey`, `UserID` query params and `User-Agent`/`Authorization` headers. Cassette spot-check: grep for the developer's real UA in cassette files → must not match.

## 3. FRED adapter

- [x] 3.1 Create `apps/api/tests/unit/contexts/research/sources/test_fred.py` — RED phase: (a) `FREDSource()` raises `ConfigError` if `FRED_API_KEY` unset; (b) given mocked observations response, adapter yields draft with `source_id="fred"`, `tier="A"`, `effective_from = observation.date`, `recorded_from = observation.realtime_start`, `dedupe_key = f"fred:{series_id}:{observation_date}:{realtime_start}"`; (c) ALFRED revision flow — first ingest yields draft with `realtime_start=T1`; second ingest with revised `realtime_start=T2` for the same observation date triggers `repository.supersede_fact(old_id, T2)` then inserts new row; (d) `observation.value == "."` is skipped silently with structlog event `research.fred.skipped_missing`; (e) rate limiter throttles to ~2 req/s.
- [x] 3.2 Create `apps/api/src/iguanatrader/contexts/research/sources/fred.py` implementing `FREDSource(TierASourceAdapter)`. Class attrs: `source_id="fred"`, `rate_limit_per_second=2.0`. Init: reads `FRED_API_KEY` env. Methods: `async def fetch_series(series_id: str, since: datetime | None) -> AsyncIterator[ResearchFactDraft]` (NOT `fetch(symbol, since)` — FRED is series-based; the base class's `fetch` raises `NotImplementedError` and concrete callers use `fetch_series`); `_query_alfred(series_id, since)` returning paginated observations with `realtime_start <= "9999-12-31"` (full vintage history); `_build_draft(observation, series_id)`; `_check_for_revision(draft)` returns existing fact id if `(series_id, observation.date)` already has a row with `recorded_to IS NULL` AND a different `realtime_start`. `_emit_with_revision` performs the supersede + insert atomically.
- [x] 3.3 Create VCR cassette `tests/fixtures/vcr/fred_cpi.yaml` (CPIAUCSL series, since=2024-01-01) — run live, scrub, commit.
- [x] 3.4 Create `tests/integration/test_fred_ingestion.py` (analogous to test_edgar_ingestion.py): (a) ingestion populates research_facts; (b) re-running inserts 0 rows; (c) ALFRED revision flow — assert that fetching a fixture cassette with two vintages of the same observation results in 2 rows where the first has `recorded_to = vintage2.realtime_start`; (d) every fact has `tier="A"` + `recorded_from` populated.

## 4. BLS adapter

- [x] 4.1 Create `tests/unit/contexts/research/sources/test_bls.py` — RED phase: (a) `BLSSource()` raises `ConfigError` if `BLS_API_KEY` unset; (b) registered-tier success returns drafts with `effective_from = period_start`, `effective_to = period_end`, `recorded_from = release_date`; (c) response with `status="REQUEST_NOT_PROCESSED"` raises `RateLimitedError` (NOT silent insert-zero); (d) period parsing covers `M01`-`M12` (monthly), `Q01`-`Q04` (quarterly), `A01` (annual); (e) `dedupe_key = f"bls:{series_id}:{year}:{period}:{recorded_from_date}"`.
- [x] 4.2 Create `apps/api/src/iguanatrader/contexts/research/sources/bls.py` implementing `BLSSource(TierASourceAdapter)`. Class attrs: `source_id="bls"`, `rate_limit_per_second=0.0058`. Init: reads `BLS_API_KEY` env. Methods: `async def fetch_series(series_id, since) -> AsyncIterator[ResearchFactDraft]` POSTing to v2 API; `_parse_period_to_dates(year, period) -> (start, end)` (M01-M12 → first-of-month → end-of-month; Q01-Q04 → quarter bounds; A01 → year bounds; S01/S02 → semi-annual; R01-R12 → "annual ranking" - skip with structlog); `_lookup_release_date(series_id, year, period)` against cached BLS calendar (TTL=24h); `_build_draft(...)`. Body must check `response_body["status"] == "REQUEST_SUCCEEDED"` before parsing data; non-success → `RateLimitedError(detail=msg)`.
- [x] 4.3 Pytest-httpx mocked tests only (no VCR cassette for BLS — registered-tier responses are large and key-tied; integration test deferred to R5 once a downstream consumer exists). Document in `tests/integration/README.md`.

## 5. BEA adapter

- [x] 5.1 Create `tests/unit/contexts/research/sources/test_bea.py` — RED phase: (a) `BEASource()` raises `ConfigError` if `BEA_API_KEY` unset; (b) NIPA T10101 GDP fetch returns drafts with `effective_from = period_start`, `effective_to = period_end`, `recorded_from = release_date`; (c) numeric strings with embedded commas (`"23,456.7"`) parse correctly to `Decimal("23456.7")`; (d) `"…"` ellipsis values are skipped with `research.bea.skipped_missing`; (e) revision tracking — given two cassettes for the same quarter at advance + second-estimate vintages, assert 2 rows with the first's `recorded_to` set.
- [x] 5.2 Create `apps/api/src/iguanatrader/contexts/research/sources/bea.py` implementing `BEASource(TierASourceAdapter)`. Class attrs: `source_id="bea"`, `rate_limit_per_second=1.66`. Init: reads `BEA_API_KEY`. Methods: `async def fetch_dataset(dataset, table, frequency, year_range) -> AsyncIterator[ResearchFactDraft]` against `apps.bea.gov/api/data/`; `_parse_value(value_str)` strips commas + handles ellipsis; `_lookup_release_date(period, vintage)` from BEA release schedule cache; `_build_draft(...)`; revision detection identical to FRED's pattern (close prior `recorded_to` via `supersede_fact`). Batch inserts in groups of 200 with explicit `session.commit()` per design risk note.
- [x] 5.3 Pytest-httpx mocked tests (no VCR for BEA initially; same rationale as BLS).

## 6. Cross-adapter property tests + token bucket unit tests

- [x] 6.1 Create `apps/api/tests/property/test_dedupe_key_uniqueness.py` using Hypothesis: generate diverse SEC accession numbers / FRED `(series_id, date, vintage)` tuples / BLS / BEA tuples; assert that no two distinct inputs collide on their adapter's `dedupe_key`. Expected: zero collisions over 10000 examples per source.
- [x] 6.2 Create `apps/api/tests/unit/contexts/research/sources/test_token_bucket.py`: assert `TokenBucket(rate=10.0)` throttles 100 sequential `acquire()` calls to ≥10 seconds wall-clock; assert two concurrent tasks sharing a class-level bucket do not exceed the rate; assert `acquire()` is fair (no task starvation under contention).
- [x] 6.3 Create `apps/api/tests/unit/contexts/research/sources/test_base_adapter.py`: assert `_request(...)` retries 5 times on 503 with the canonical `[3,6,12,24,48]` waits (use `freezegun` or `time-machine` to assert sleep durations); assert 404 yields no retry + emits `permanent_skip`; assert 429 with `Retry-After: 30` honours that header; assert `_make_draft` always sets `tier='A'` + `retrieval_method='api'` + `retrieved_at` within ±60s of `time.utc_now()`.

## 7. Documentation + verification

- [x] 7.1 Append to `docs/gotchas.md` (deviation-aware: use next available number after R1's #43 — likely #44+). Gotchas: SEC EDGAR mandatory User-Agent format; BLS unregistered-tier silent rate-limit (must check `status` in body, not HTTP status); FRED ALFRED `realtime_start` is a date in ET, treated as `T00:00:00 ET → UTC`; BEA NIPA values may contain commas + ellipsis-for-missing; rate-limit token buckets are class-level (single-process only — multi-worker coordination is out of MVP); VCR cassette scrubbing config + recording procedure.
- [x] 7.2 Update `apps/api/README.md` with a "Tier-A research sources (R2)" subsection: required env vars (`SEC_EDGAR_USER_AGENT`, `FRED_API_KEY`, `BLS_API_KEY`, `BEA_API_KEY`); how to register for each (links to SEC, FRED, BLS, BEA signup pages); how to record a fresh VCR cassette (`make record-cassettes`).
- [x] 7.3 Update `docs/getting-started.md` "Tier-A research keys" section: documents the four env vars + their format requirements + free-registration instructions per source. Cross-reference `docs/gotchas.md` entries from 7.1.
- [x] 7.4 Append stub entries to `secrets/dev.env.enc` template (encrypted with SOPS/age per slice 1's `.sops.yaml`): `SEC_EDGAR_USER_AGENT="iguanatrader-dev your-email@example.com"`, `FRED_API_KEY="DUMMY"`, `BLS_API_KEY="DUMMY"`, `BEA_API_KEY="DUMMY"`. Real values per developer; `DUMMY` makes adapter init fail-fast at runtime which is the intended behaviour for an unconfigured dev env.
- [x] 7.5 `mypy --strict apps/api/src/iguanatrader/contexts/research/sources/ apps/api/src/iguanatrader/contexts/research/errors.py apps/api/src/iguanatrader/migrations/versions/0004_research_dedupe_index.py` — must report zero issues.
- [x] 7.6 `pytest apps/api/tests/unit/contexts/research/sources/ apps/api/tests/integration/test_edgar_ingestion.py apps/api/tests/integration/test_fred_ingestion.py apps/api/tests/property/test_dedupe_key_uniqueness.py -v` — all green. (Per project convention, may defer to CI if Windows hangs; document in tasks 7.6.)
- [x] 7.7 Coverage on the listed modules ≥ 80% (`pytest --cov=contexts.research.sources --cov-report=term-missing`). Surface coverage estimate target: ≥90% (each adapter has ~250 LOC + dedicated tests).
- [x] 7.8 `pre-commit run --from-ref origin/main --to-ref HEAD` — gitleaks must pass against committed cassettes (verify scrubbing); ruff + black + mypy clean; `openapi-typescript` regen is no-op (R2 adds no routes).
- [x] 7.9 `alembic upgrade head` clean against fresh SQLite + `alembic downgrade -1` reverses cleanly. Verify the partial unique index on `(tenant_id, dedupe_key) WHERE dedupe_key IS NOT NULL` is created (sqlite: `PRAGMA index_list('research_facts')` includes the index; postgres: `\d research_facts` shows it).
- [x] 7.10 Confirm no shared infra files were edited via `git diff --name-only origin/main...HEAD`. Allowed: `apps/api/src/iguanatrader/contexts/research/sources/*` (NEW), `apps/api/src/iguanatrader/contexts/research/errors.py` (additive new error classes), `apps/api/src/iguanatrader/migrations/versions/0004_research_dedupe_index.py` (NEW), tests under `apps/api/tests/{unit,integration,property}/contexts/research/sources/` and the two integration test files at the integration root, `apps/api/tests/fixtures/vcr/*.yaml` (NEW), `apps/api/README.md` + `docs/gotchas.md` + `docs/getting-started.md` (additive), `secrets/dev.env.enc` (additive entries), `pyproject.toml` (dev deps `vcrpy` + `pytest-vcr`), `openspec/changes/research-edgar-fred-adapters/*`. Forbidden: `routes/__init__.py`, `sse/__init__.py`, `cli/main.py`, `app.py`, `errors.py` (shared), `persistence/*.py`, `contexts/research/repository.py` (R1's), `contexts/research/ports.py` (R1's), `contexts/research/models.py` (R1's). Anti-collision compliance: full.
- [x] 7.11 PR description "AI-reviewer signoff" subsection — record any deviations (e.g., migration slot not 0004; choice between dedupe-wrapper vs additive insert_fact param). PR creation is post-task per the standard slice workflow.
