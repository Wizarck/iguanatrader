## 1. Sidecar package skeleton (license + project metadata)

- [x] 1.1 Create `apps/openbb-sidecar/pyproject.toml` declaring `[tool.poetry] name = "openbb-sidecar"`, `version = "0.1.0"`, `license = "AGPL-3.0-or-later"` (SPDX), `description = "AGPL-isolated OpenBB Platform sidecar for iguanatrader; communicates with the iguanatrader monolith over HTTP loopback per ADR-015."`, Python `^3.11`. Declare runtime deps: `openbb = "^4"`, `openbb-equity`, `openbb-economy`, `fastapi = "^0.115"`, `uvicorn = {extras = ["standard"], version = "^0.32"}`, `httpx = "^0.27"`, `structlog = "^24.4"`, `pydantic-settings = "^2.6"`. Dev deps: `pytest = "^8"`, `pytest-asyncio = "^0.24"`, `mypy = "^1.13"`, `ruff = "^0.7"`. **Deviation**: python pin tightened to `>=3.11,<3.13` because openbb sub-packages restrict Python to <3.13.
- [x] 1.2 Generate `apps/openbb-sidecar/poetry.lock` via the `regenerate-lock.yml` workflow_dispatch (slice 1 pattern; per gotcha #18 local poetry path is fragile). **Deviation**: extended `regenerate-lock.yml` to a matrix strategy covering both root (Python 3.13) and `apps/openbb-sidecar/` (Python 3.12, openbb-equity restriction). Bug-fix pass needed on first run: `git diff --quiet` silently passed on untracked files; replaced with `git add` + `git diff --cached --quiet`. Lock landed at 4827 lines (commit `86d02cb`).
- [x] 1.3 Create `apps/openbb-sidecar/LICENSE` containing AGPL-3.0 v3 verbatim text sourced from https://www.gnu.org/licenses/agpl-3.0.txt. This is a SEPARATE file from the repo root `LICENSE` (Apache-2.0 + Commons Clause). Do NOT modify the root LICENSE.
- [x] 1.4 Create `apps/openbb-sidecar/README.md` opening with the AGPL-3.0 attribution one-liner per design D6 ("This sidecar embeds OpenBB Platform..."), followed by sections: Purpose (license isolation, link to ADR-015), Endpoints (5 endpoints listed with method+path), Local development (how to run standalone for testing), License (full attribution), and a final note pointing to ADR-015 for the boundary rationale.
- [x] 1.5 Create `apps/openbb-sidecar/.dockerignore` excluding `tests/`, `__pycache__`, `.pytest_cache`, `.mypy_cache`, `.ruff_cache`, `*.pyc`, `.git/`, `.venv/`. Keep the AGPL LICENSE inside the image (required by AGPL §13).
- [x] 1.6 Create `apps/openbb-sidecar/Dockerfile` (multi-stage): `builder` stage from `python:3.11-slim` running `poetry install --only main --no-root`; `runtime` stage from `python:3.11-slim` copying the venv + the sidecar source + the LICENSE file. `EXPOSE 8765`. `CMD ["uvicorn", "openbb_sidecar.main:app", "--host", "0.0.0.0", "--port", "8765"]`. Add `HEALTHCHECK --interval=10s --timeout=5s --start-period=60s --retries=12 CMD curl -fsS http://localhost:8765/health || exit 1`.

## 2. Sidecar FastAPI app + /health route

- [x] 2.1 Create `apps/openbb-sidecar/src/openbb_sidecar/__init__.py` with package docstring + `__version__ = "0.1.0"`.
- [x] 2.2 Create `apps/openbb-sidecar/src/openbb_sidecar/config.py` using `pydantic-settings.BaseSettings` reading env vars: `OPENBB_SIDECAR_HOST` (default `"0.0.0.0"`), `OPENBB_SIDECAR_PORT` (default `8765`), `OPENBB_SIDECAR_LOG_LEVEL` (default `"INFO"`). No SOPS layer in MVP per design D10.
- [x] 2.3 Create `apps/openbb-sidecar/src/openbb_sidecar/main.py`: FastAPI app factory `create_app() -> FastAPI`, configures structlog with the level from config, mounts the dynamic-discovery `register_routers(app)` helper from `routes/__init__.py`. `app = create_app()` at module level so uvicorn can import it.
- [x] 2.4 Create `apps/openbb-sidecar/src/openbb_sidecar/adapters/__init__.py` (empty package marker) + `adapters/openbb_facade.py`. The facade exposes a class `OpenBBFacade` with methods `is_ready() -> bool` (lazy-imports openbb on first call, caches result), `equity_fundamentals(symbol: str) -> dict`, `equity_ratings(symbol: str) -> dict`, `equity_esg(symbol: str) -> dict`, `economy_macro(indicator: str) -> dict`. **This is the ONLY module inside the sidecar that imports openbb** (per design D5). All imports of `openbb` are lazy (inside method bodies) so module-level import is cheap. **Note**: data methods raise `NotImplementedError` until R4 group 3 wires real openbb endpoints; routes (group 3) will exercise the real path.
- [x] 2.5 Create `apps/openbb-sidecar/src/openbb_sidecar/routes/__init__.py` with `register_routers(app: FastAPI)` that iterates `pkgutil.iter_modules(__path__)` and `app.include_router(module.router)` for each module exporting a top-level `router: APIRouter` (anti-collision pattern, mirrors slice 5 D1). Emit structlog `openbb_sidecar.router.registered` / `.skipped` / `.import_failed` per slice 5 contract.
- [x] 2.6 Create `apps/openbb-sidecar/src/openbb_sidecar/routes/health.py` exporting `router: APIRouter`. `GET /health` returns 200 with body `{"status": "ok", "openbb_loadable": OpenBBFacade().is_ready(), "version": __version__}`. Healthcheck NEVER returns 5xx — a sidecar that cannot import openbb returns 200 with `openbb_loadable: false` so docker healthcheck still passes container-alive but the operator sees the readiness flag is false.
- [x] 2.7 Create `apps/openbb-sidecar/tests/__init__.py` + `apps/openbb-sidecar/tests/unit/test_health_route.py`: pytest-asyncio + httpx ASGITransport client. 3 tests: shape assertion, openbb-import-fails path (loadable=false + 200), import caching (single import for repeated /health calls).
- [~] 2.8 Verify image builds: docker build + run + curl. **DEFERRED to Group 4** — the docker-compose integration (group 4) ships the canonical `docker compose up -d openbb_sidecar` smoke test which validates the same image lifecycle end-to-end. Standalone `docker build` adds friction without coverage gain.

## 3. Sidecar routes /v1/equity/* + /v1/economy/*

- [ ] 3.1 Create `apps/openbb-sidecar/src/openbb_sidecar/routes/equity.py` exporting `router: APIRouter` with prefix `/v1/equity`. Endpoints: `GET /fundamentals/{symbol}` → calls `OpenBBFacade().equity_fundamentals(symbol)`, returns 200 with at least `{symbol, pe_ratio, market_cap, dividend_yield, as_of_date}`; `GET /ratings/{symbol}` → returns `{symbol, consensus, target_price, analyst_count, as_of_date}`; `GET /esg/{symbol}` → returns `{symbol, esg_score, environmental_score, social_score, governance_score, as_of_date}`. On unknown symbol or upstream OpenBB error: 404 / 502 with structured detail. Wrap every facade call in try/except; let only HTTPException propagate.
- [ ] 3.2 Create `apps/openbb-sidecar/src/openbb_sidecar/routes/economy.py` exporting `router: APIRouter` with prefix `/v1/economy`. Endpoint: `GET /macro/{indicator}` → calls `OpenBBFacade().economy_macro(indicator)`, returns 200 with `{indicator, series, unit, frequency}`. 404 on unknown indicator; 502 on upstream OpenBB error.
- [ ] 3.3 Create `apps/openbb-sidecar/tests/unit/test_openbb_facade.py`: mocks the lazy openbb import; asserts each facade method maps inputs → expected output shapes; asserts errors propagate as expected exceptions (rather than crashing the process).
- [ ] 3.4 Create `apps/openbb-sidecar/tests/integration/test_routes.py`: ASGITransport client tests for each endpoint with the facade methods mocked. Asserts route → facade → response shape end-to-end without hitting the real OpenBB SDK (CI-fast, no network).
- [ ] 3.5 Smoke test against the live sidecar (manual or scripted): `docker compose up -d openbb_sidecar` (after section 4 lands), `curl http://localhost:8765/v1/equity/fundamentals/AAPL` returns 200 with sane numeric fields. Capture sample response into `apps/openbb-sidecar/tests/fixtures/aapl_fundamentals.json` for future contract tests.

## 4. docker-compose integration

- [ ] 4.1 Edit `docker-compose.yml` (dev): add an `openbb_sidecar` service block: `build.context: ./apps/openbb-sidecar`, `build.dockerfile: Dockerfile`, `image: iguanatrader/openbb-sidecar:dev`, `expose: ["8765"]` (NOT `ports:` — must NOT bind to host per design D1 + risk mitigation), `restart: unless-stopped`, `healthcheck: {test: ["CMD", "curl", "-fsS", "http://localhost:8765/health"], interval: 10s, timeout: 5s, retries: 12, start_period: 60s}`. Update existing `api` service to add `depends_on: { openbb_sidecar: { condition: service_healthy } }`.
- [ ] 4.2 Edit `docker-compose.paper.yml`: same `openbb_sidecar` block but `image: iguanatrader/openbb-sidecar:paper`, `restart: always`, `start_period: 90s` (allow more warmup in paper-trading profile). Same `depends_on` clause on the api service.
- [ ] 4.3 Edit `docker-compose.live.yml`: same as paper but `image: iguanatrader/openbb-sidecar:live`, `start_period: 120s`. Live profile has stricter memory limits (per architecture-decisions.md §"OpenBB Sidecar Topology": idle ~500 MB, peak ~1.5 GB) — set `mem_limit: 2g`, `mem_reservation: 1g`.
- [ ] 4.4 Do NOT edit `docker-compose.test.yml` — sidecar is mocked in unit tests; the dev compose covers integration. Verify with `docker compose -f docker-compose.test.yml config` that no sidecar references slipped in.
- [ ] 4.5 Verify the host port is NOT bound: `docker compose up -d openbb_sidecar` → `docker compose port openbb_sidecar 8765` should return empty / `<no entry>`. Document this verification step in `apps/openbb-sidecar/README.md` (operators frequently misread `expose` vs `ports` in compose).
- [ ] 4.6 Verify boot order: `docker compose up -d` (with both api + sidecar) → `docker compose ps` shows `openbb_sidecar` `(healthy)` then `api` started. Bring down via `docker compose down`.

## 5. Monolith HTTP client (apps/api/contexts/research/sources/openbb_sidecar.py)

- [ ] 5.1 Create `apps/api/src/iguanatrader/contexts/research/sources/__init__.py` (empty package marker; the `sources/` subpackage is created by R4 — first slice that needs it).
- [ ] 5.2 Create `apps/api/src/iguanatrader/contexts/research/sources/openbb_sidecar.py` defining `class OpenBBSidecarSource:` implementing R1's `SourcePort` Protocol. Attributes: `_client: httpx.AsyncClient` (single shared instance per source), `_base_url: str` (default `"http://localhost:8765"`, override via `OPENBB_SIDECAR_URL` env), `_enabled: bool` (default True, override via `OPENBB_SIDECAR_ENABLED=false` env to disable adapter without removing it from the source registry). Inherits `HeartbeatMixin` from `iguanatrader.shared.heartbeat` for liveness pings.
- [ ] 5.3 Implement `OpenBBSidecarSource.fetch(symbol, since)` per design D3: when `_enabled is False` → yield nothing + return immediately; otherwise issue HTTP GET to `/v1/equity/fundamentals/{symbol}`, `/v1/equity/ratings/{symbol}`, `/v1/equity/esg/{symbol}` (parallel via `asyncio.gather`); for each successful response, JSON-parse + build `ResearchFactDraft` via `with_payload(payload_bytes)` for hybrid-storage tier dispatch; yield each draft.
- [ ] 5.4 Implement retry/backoff per design D3 + slice 2's canonical `[3, 6, 12, 24, 48]` schedule from `iguanatrader.shared.backoff`: 5xx → retry; 4xx → log warning + return empty for that endpoint (do NOT retry 4xx); connection error / timeout → retry; final-attempt failure → emit structlog `research.openbb_sidecar.unreachable` + raise `IntegrationError` from `iguanatrader.shared.errors`.
- [ ] 5.5 Create `apps/api/src/iguanatrader/contexts/research/sources/yfinance_proxy.py` implementing `SourcePort` per design D7. Routes through OpenBBSidecarSource's endpoints (`/v1/equity/fundamentals/...`, `/v1/equity/esg/...`) — no direct `import yfinance` anywhere in `apps/api/`. Same retry/backoff/heartbeat shape as `openbb_sidecar.py`.
- [ ] 5.6 Verify mypy strict: `mypy --strict apps/api/src/iguanatrader/contexts/research/sources/` clean.

## 6. License-boundary CI workflow update

- [ ] 6.1 Edit `.github/workflows/license-boundary-check.yml` `agpl-boundary` job: replace the slice-1 placeholder no-op step with three real assertions per design D4:
   - Step "Assert no AGPL declared in apps/api/pyproject.toml": `grep -iE '^\s*openbb' apps/api/pyproject.toml` → if match, `::error::` + exit 1.
   - Step "Assert no AGPL packages in apps/api/poetry.lock": `grep -iE '^name = "openbb' apps/api/poetry.lock` → if match, `::error::` + exit 1.
   - Step "Assert no openbb imports in apps/api/src/ or packages/": `rg -nE '^(from|import)\s+openbb(\.|$|\s)' apps/api/src/ packages/` → if match, `::error::` + exit 1.
- [ ] 6.2 Remove the slice-1 "Skip until slice R4" guard `if: ${{ hashFiles('apps/openbb-sidecar/**/*') == '' }}` from the `agpl-boundary` job — once R4 lands, the gate must always run.
- [ ] 6.3 Add a NEGATIVE test for the gate: branch `test/agpl-boundary-leak-detection` with a stub line `openbb = "^4"` injected into `apps/api/pyproject.toml`; push, verify the workflow run FAILS with the expected `::error::` message; revert the stub before merge. Document the verification step (PR comment + screenshot) in the slice's PR body.
- [ ] 6.4 Add a POSITIVE test on `main`: confirm the `agpl-boundary` job passes on the slice's PR (no leakage in the slice's own changes). The PR's CI run is the audit trail.

## 7. Integration test (monolith client → live sidecar → research_facts)

- [ ] 7.1 Create `apps/api/tests/integration/test_openbb_sidecar_client.py`: pytest-asyncio + httpx ASGITransport against a *mock* sidecar (canned JSON responses) by default. Test cases: (a) happy path — fetch returns ≥1 ResearchFactDraft with correct `source_id`, `source_url`, `value_jsonb`, `recorded_from`; (b) sidecar 5xx → retries per backoff schedule then raises IntegrationError; (c) sidecar 404 → empty iterable + WARN log; (d) connection refused → retries then IntegrationError; (e) `OPENBB_SIDECAR_ENABLED=false` → fetch yields nothing without making any HTTP calls.
- [ ] 7.2 Add an opt-in marker `@pytest.mark.sidecar_live` for tests that boot the docker-compose `openbb_sidecar` service and hit the live OpenBB SDK. Wire the marker in `pyproject.toml` under `[tool.pytest.ini_options] markers`. CI runs this set in a separate job to keep the default test job fast.
- [ ] 7.3 Create `apps/api/tests/integration/test_openbb_sidecar_e2e.py` (marked `@pytest.mark.sidecar_live`): boots compose, calls `OpenBBSidecarSource(...).fetch("AAPL", since=None)`, asserts ≥1 ResearchFactDraft with `source_id="openbb-sidecar"`, persists via `ResearchRepository.insert_fact`, queries back the row + asserts bitemporal columns populated correctly.
- [ ] 7.4 Update `.github/workflows/ci.yml` to add a `sidecar-tests` job that runs `cd apps/openbb-sidecar && poetry install && poetry run pytest` (the sidecar's own pytest suite, separate venv per design D9). Run in parallel with the existing `api-tests` job.

## 8. Documentation + verification

- [ ] 8.1 Append to `docs/gotchas.md`: gotcha #N — sidecar's `openbb` lazy-import means cold-start `/health` returns 200 immediately with `openbb_loadable: false`; readiness comes ~5-15s later when the import completes. Tune docker healthcheck `start_period` accordingly. Gotcha #N+1 — `expose:` not `ports:` in compose; `docker compose port openbb_sidecar 8765` MUST return empty (verifies host binding NOT happening). Gotcha #N+2 — yfinance fundamentals/ratings/ESG flow through OpenBB sidecar, not direct yfinance import in apps/api/; design D7 rationale.
- [ ] 8.2 Update `docs/adr/ADR-015-2026-04-28-openbb-sidecar-isolation.md`: transition `status: proposed` → `status: accepted`, expand the §"Full content" section with the actual decision body now that R4 has shipped (Context, Decision, Consequences). Reference this slice's design.md as the authoritative implementation record.
- [ ] 8.3 Append to `docs/architecture-decisions.md` §"OpenBB Sidecar Topology" a "**Implementation status: ✅ R4 (2026-05-06)**" line near the top of the section.
- [ ] 8.4 Run pre-commit: `pre-commit run --from-ref origin/main --to-ref HEAD` — green.
- [ ] 8.5 Run mypy strict: `mypy --strict apps/api/src/iguanatrader/contexts/research/sources/` AND `cd apps/openbb-sidecar && poetry run mypy --strict src/` — both clean.
- [ ] 8.6 Run pytest: `pytest apps/api/tests/integration/test_openbb_sidecar_client.py` (default marker set, no sidecar_live) — green. `cd apps/openbb-sidecar && poetry run pytest` — green.
- [ ] 8.7 Run the live integration test: `pytest -m sidecar_live apps/api/tests/integration/test_openbb_sidecar_e2e.py` after `docker compose up -d openbb_sidecar` — green.
- [ ] 8.8 Verify the license-boundary CI gate: workflow run on the slice's PR shows `agpl-boundary` job ✅ green; the negative-test branch (task 6.3) shows ❌ red with the expected error message.
- [ ] 8.9 Manual smoke (operator perspective): from a fresh `docker compose up -d`, wait for both services to show `(healthy)`. `curl http://localhost:8765/health` → 200 + `openbb_loadable: true`. `curl http://localhost:8765/v1/equity/fundamentals/AAPL` → 200 with sane fields. Bring down: `docker compose down`.
- [ ] 8.10 PR description includes "AI-reviewer signoff" subsection per release-management.md §4.5; populate self-review findings + L1 detection result. Note explicitly: "this slice plants the AGPL-3.0 ↔ Apache+CC license boundary; CI gate is hard-blocking on the three independent surfaces per design D4."
