apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "iguanatrader.fullname" . }}-env
  labels:
    {{- include "iguanatrader.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
