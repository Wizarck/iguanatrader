---
description: "iguanatrader dispatcher — Cursor router pointing at AGENTS.md."
alwaysApply: true
---

Read `AGENTS.md` at the project root first. That is the project dispatcher; it carries the bootstrap directive (§0), capability map (§5), and pointers to all other context.

Universal norms inherit from `.ai-playbook/specs/*` (git submodule pinned in `AGENTS.md` frontmatter).

This rule is the thin router. Project-specific guidance lives in `AGENTS.md` + `docs/`.
