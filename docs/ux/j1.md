---
journey: J1 — Authenticated user portfolio surface
status: canonical (MVP)
persona: Arturo Ramírez (sole user — see personas-jtbd.md)
device: desktop primary (Windows 11 Pro localhost), tablet/mobile secondary
mock: deferred — Sally to author docs/ux/variants/mock-j1-*.html before slice 4 starts
last-updated: 2026-05-01
parent: docs/ux/
related:
  - components.md
  - j0.md
  - j2.md
  - j3.md
  - ../prd.md (Journey 1 narrative + FR54 + FR55)
  - ../architecture-decisions.md (§Frontend Architecture, §Authentication & Security)
  - ../personas-jtbd.md (JTBD-1, JTBD-2, JTBD-6)
  - ../openspec-slice.md (rows 4 auth-jwt-cookie, W1 dashboard-svelte-skeleton, T4 trading-routes-and-daemon)
---

# Journey 1 — Authenticated user portfolio surface (DRAFT v1)

> Source: PRD §User Journeys / Journey 1 (happy path, narrated as Telegram approval but the dashboard is the persistent surface throughout); FR31, FR38 (auth + authorized-senders enforcement), FR46-FR49 (persistence), FR54 (dashboard surface), FR55 (kill-switch button), FR12-FR18 (trade lifecycle visible in dashboard), FR1-FR5 (strategy management surface). Personas: JTBD-1 (hands-off pipeline + veto power), JTBD-2 (programmatic risk policy), JTBD-6 (trustworthy unattended). Architecture Decisions: cookie-based JWT session (HttpOnly + Secure + SameSite=Strict), 7-day sliding lifetime, SvelteKit `(app)` route group authenticated-only.

## 1. Goal

Enable Arturo to authenticate once via the web login surface, then live inside the authenticated dashboard as the **persistent operational surface** — observing portfolio state, equity curve, open positions, trade history, and active strategies — while the autonomous proposal pipeline runs in the background and surfaces its outputs through Journey 2 (approval flow) and Journey 3 (research briefings + weekly review).

This journey covers everything the user needs **between** approval moments: the read-mostly surface that makes Arturo trust the system enough to step away (JTBD-6).

## 2. Trigger

- **Cold start**: Arturo opens `https://localhost:<port>/` from his Windows host. JWT cookie missing or expired → redirected to `/login`.
- **Warm session**: JWT cookie valid (≤7 days since last activity) → redirected to `/` dashboard.
- **Tab return**: SvelteKit hydrated tab regains focus → `riskStore` + `equityStore` reconnect to SSE; stale-data banner if reconnection takes >2s.

## 3. Walkthrough

> **Step 0 (login + rate-limit + 401 redirect) lives in [j0.md](j0.md)** as of 2026-05-05. Slice 4 (`auth-jwt-cookie`) cites `j0.md`; J1 starts at the authenticated home.

### Step 1 — Authenticated home (`/`)

| User action | System response | FR cited | Components |
|---|---|---|---|
| Lands on `/` authenticated. | `(app)/+layout.server.ts` resolves `tenant_id` from JWT claim → propagates to `tenantStore`; `+layout.svelte` renders Sidebar + top bar with KillSwitchButton (compact); SSE connections open via `useEventSource`. | FR54 | `Sidebar.svelte`, `KillSwitchButton.svelte` (compact), `ConnectionIndicator.svelte` |
| Sees the home dashboard summary above the fold: equity curve (last 30d), drawdown gauge, top-3 pending approvals (preview cards), risk caps bar, today's P&L net of cost. | `+page.svelte` loads `/api/v1/portfolio/summary` + opens SSE on `/sse/equity` + `/sse/approvals` + `/sse/risk`. | FR54, FR42 (cost-per-trade ratio displayed), FR12-FR18 (pending approvals visible) | `EquityCurve.svelte`, `DrawdownGauge.svelte`, `RiskCapsBar.svelte`, `ApprovalCard.svelte` (compact), `Card.svelte` |
| Sees connection state (green dot + "Live"). | `ConnectionIndicator` reads `connectionStore`. | FR54 | `ConnectionIndicator.svelte` |
| When SSE drops momentarily. | Indicator turns amber + "Reconnecting" + Spinner; equity curve last-tick freezes; no toast spam. After 5s of disconnect: red + persistent banner at top of main region. | FR54, NFR-R7 (resilience surfaces inform user) | `ConnectionIndicator`, banner |

**Anchors slice W1 will cite**:
- `j1.md#step-1-authenticated-home` — the route surface contract for the home `+page.svelte`.
- `components.md#sidebar-svelte` — the dynamic-enumeration anti-collision pattern.
- `components.md#killswitchbutton-svelte` — global affordance present in every `(app)/*` page header.
- `components.md#layout--navigation` — `(app)/+layout.svelte` contract.

### Step 2 — Portfolio detail (`/portfolio`)

| User action | System response | FR cited | Components |
|---|---|---|---|
| Clicks "Portfolio" in Sidebar. | Active state shifts to Portfolio; `/portfolio/+page.svelte` loads `/api/v1/portfolio` + SSE for live ticks. | FR54 | `Sidebar.svelte` |
| Sees positions table (symbol, qty, avg cost, market value, unrealised P&L, % allocation), equity curve full-screen mode, drawdown over selected window, day P&L card, week P&L card, broker connection status (TWS Gateway up/down + last heartbeat). | Positions stream via SSE; `StockRow.svelte` instances animate `flip` on reorder. | FR54, FR16 (broker reconciliation surfaced), NFR-P8 (heartbeat visible) | `PositionsTable.svelte`, `StockRow.svelte`, `EquityCurve.svelte` |
| Hovers over a position. | Tooltip shows (open date, strategy that opened it, `research_brief_id` link, current stop, distance to stop in %). | FR74 (proposal-to-brief linkage), FR2 (per-symbol strategy traceability) | `PositionsTable.svelte` |
| Clicks a position. | Navigates to `/trades?position_id=<id>` filtered to that position's history (entries/exits). Out-of-scope here — covered in Step 3. | FR54 | — |
| Triggers force-exit via the row's overflow menu. | Confirmation dialog ("Type SELL to confirm") → POST `/api/v1/positions/<id>/force-exit` → runs `RiskEngine.evaluate(..., mode='exit')` per [j2.md](j2.md) Step 5 → enqueues a confirm message in approval channel + dashboard on success. | FR17 (force-exit) | `Button.svelte` (destructive), confirmation dialog |

**Anchors slice T4 will cite**:
- `j1.md#step-2-portfolio-detail` — the `/portfolio` surface contract (live positions, force-exit affordance).
- `components.md#section-5-already-defined-dashboard-atoms` — for `PositionsTable.svelte` etc. once T4 brings them online.

### Step 3 — Trade history (`/trades`)

| User action | System response | FR cited | Components |
|---|---|---|---|
| Clicks "Trades" in Sidebar. | `/trades/+page.svelte` loads `/api/v1/trades?range=last-90d&limit=200`. | FR54, FR46 (append-only persistence visible) | `Sidebar.svelte` |
| Sees filterable table (date range picker, symbol filter, strategy filter, state filter, override marker filter). Columns: timestamp ISO 8601 UTC, symbol, side, qty, fill price, P&L, holding period, strategy, override marker (badge if FR48 override applied). | Server-side pagination; client-side facet UI; CSV/PDF export button (FR50) per filter selection. | FR50 (export), FR48 (override marker), FR46 | `TradesTable.svelte`, filter bar (composes `Input` + `Badge`-as-checkbox), `Button.svelte` (Export CSV / Export PDF) |
| Clicks a row. | Drawer opens with full trade detail (every order leg, every fill, link to original `TradeProposal` + `research_brief_id` (FR74), risk evaluation snapshot, audit log entries). URL becomes `/trades?<filters>&detail=<trade_id>` via SvelteKit `goto({ keepFocus: true })` — modal-route hybrid pattern. Closing the drawer (Esc / X / backdrop click) drops the `detail` param while preserving filters; deep-link with `detail` re-mounts the drawer over current filters; browser back button closes the drawer first, then exits filters. | FR47 (config diff), FR74 (proposal linkage), FR70 (audit_trail accessible from any trade) | drawer panel composing existing atoms |
| Exports range. | Confirmation modal "Export N trades to <CSV|PDF>"; streams file. | FR50 | `Button.svelte`, modal |

**Anchors slice T4 will cite**:
- `j1.md#step-3-trade-history` — `/trades` surface contract.
- `j3.md#step-2-research-brief-detail` — for the drilldown into `research_brief_id` from a trade row.

### Step 4 — Strategies (`/strategies`)

| User action | System response | FR cited | Components |
|---|---|---|---|
| Clicks "Strategies" in Sidebar. | `/strategies/+page.svelte` loads `/api/v1/strategies` (admin) or read-only mirror (user). | FR1 (list strategies + versions) | `Sidebar.svelte` |
| Sees per-strategy card: name, version, applicable symbols (chip list), enabled/disabled toggle, params summary, recent performance (small sparkline of equity contribution last 30d). | Read from `strategy_configs` per tenant. | FR1, FR2 (per-symbol enable/disable visible) | `Card.svelte`, `Badge.svelte` (state pill), small sparkline (composes `EquityCurve` minimal mode) |
| Toggles enabled/disabled on a symbol. | Confirmation popover; server PATCHes `strategy_configs`; toast "Strategy DonchianATR disabled for SPY — open positions unaffected" (FR2 explicit guarantee). | FR2 | `Button.svelte`, `Badge.svelte`, toast |
| Edits parameters via "Edit YAML" affordance. | Modal with `YAMLEditor.svelte` (CodeMirror 6 wrapper, lazy-loaded on modal open); client-side syntax + indent + basic lint via `@codemirror/lang-yaml`; server-side schema validation is definitive. On save POSTs `/api/v1/strategies/<id>/config` which writes `config_changes` (FR47). | FR3 (per-symbol params via yaml or runtime), FR4 (hot-reload), FR47 (config diff persistence) | modal, `YAMLEditor.svelte` (CodeMirror 6 minimal — locked 2026-05-05), `Button.svelte` |
| Issues runtime override via approval channel command. | Out-of-band — surfaces in `/strategies` as a "recent overrides" panel pulling from `config_changes`. | FR5 (runtime param override) | inline list |

**Anchors slice T4 will cite**:
- `j1.md#step-4-strategies` — `/strategies` surface contract.

### Step 5 — Logout / session expiry

| User action | System response | FR cited | Components |
|---|---|---|---|
| Clicks user menu in Sidebar footer → "Sign out". | POST `/api/v1/auth/logout` clears cookie; redirect to `/login`. | FR31 | `Sidebar.svelte` user menu |
| Tab idle >7 days. | Next API call returns 401 → hand-off to [j0.md](j0.md) Step 2 (session-expired banner + redirect-to preservation). | FR31, FR38 | hooks redirect path |
| Tab loses connection >30s (laptop sleep). | Reconnect on resume; if cookie still valid, seamless; if not, follow [j0.md](j0.md) Step 2. | NFR-R7 | `ConnectionIndicator.svelte` |

**Anchors slice 4 will cite**:
- `j1.md#step-5-logout--session-expiry` — logout flow (the act of terminating an authenticated session, originated from inside J1).
- `j0.md#step-2-session-expired-loginredirect_to-with-banner` — passive expiry surface (cited from J1 but owned by J0).

## 4. Components used

`Sidebar.svelte`, `(app)/+layout.svelte`, `KillSwitchButton.svelte`, `Button.svelte`, `Input.svelte` (filter bars, strategy edit modal), `Card.svelte`, `Badge.svelte`, `Spinner.svelte`, `EmptyState.svelte`, `ConnectionIndicator.svelte`, `EquityCurve.svelte`, `DrawdownGauge.svelte`, `PositionsTable.svelte`, `StockRow.svelte`, `TradesTable.svelte`, `RiskCapsBar.svelte`, `ApprovalCard.svelte` (compact preview on home), `SkeletonLoader.svelte`, `Toast.svelte`. See [components.md](components.md) for full contracts. `+error.svelte` and the `(auth)/login/+page.svelte` are owned by [j0.md](j0.md).

## 5. Capabilities satisfied

- **Auth & RBAC**: FR31, FR38 (and personas-jtbd.md RBAC matrix admin/user).
- **Dashboard surface**: FR54, FR55 (kill-switch from anywhere).
- **Trade lifecycle visible**: FR12-FR18 (proposals + force-exit + force-create), FR46 (append-only history), FR48 (override markers visible).
- **Strategy management surface**: FR1-FR5.
- **Persistence audit visible**: FR47 (config diffs), FR70 (audit_trail reachable from any trade row).
- **Cost observability**: FR42 (cost-per-trade ratio in home summary).
- **Export**: FR50.
- **Resilience**: NFR-P8 (heartbeat surfaced), NFR-R7 (SSE reconnect transparency).

## 6. Edge cases

- **Approval arrives mid-Journey-1** → see [j2.md](j2.md). The compact `ApprovalCard` preview on the home page is the bridge.
- **Research drilldown from a trade row** → see [j3.md](j3.md) Step 2 and Step 3.
- **TWS Gateway disconnect** → mirrored from PRD Journey 4 (failure recovery). The `ConnectionIndicator` + RiskCapsBar surface convey state; the Telegram/WhatsApp Tier-1 alert is the proactive channel — not duplicated as a toast in the dashboard.
- **Multi-tab open**: `tenantStore` + JWT cookie shared; SSE connections per-tab. Each tab can run independent filters/views; mutating actions (approve, override, force-exit) propagate via SSE so all tabs converge.
- **Cold-tab reconnect after suspend** (laptop closed): freeze data; show "stale" banner; SSE reconnects with last-event-id to backfill missed events.

## 7. Decisions specific to J1

- **Top-of-page kill-switch is global, not per-page**. Compact KillSwitchButton in the (app) top bar is wired once at the layout level (per [components.md](components.md#layout--navigation)). Rationale: every screen must allow halting the system in 1 action (FR55 + JTBD-6 trust).
- **Auth route group `(auth)/`** lives in [j0.md](j0.md); J1 owns the `(app)/` group only.
- **Active-route prefix matching in Sidebar**: deep routes like `/research/AAPL/audit-trail/3` keep "Research" highlighted (decision rationale in [components.md](components.md#22-sidebarsvelte) Sidebar Behaviour).
- **No PWA in MVP**: per Architecture Decisions §Frontend Architecture, `vite-plugin-pwa` configured but disabled until v2 hosts dashboard outside localhost.
- **Theme toggle in Sidebar footer** (not user menu) — light/dark + system preference per Architecture Decisions §Frontend Architecture.
- **Date format ISO 8601 absolute + relative pair** everywhere (per memory `feedback_date_format_preference.md`). No relative-only ("3h ago") without the absolute timestamp companion.

## 8. Notes for implementation

- **Slice 4 (`auth-jwt-cookie`)** lands the `(auth)/login/+page.svelte`, the JWT cookie middleware, the redirect-to behaviour, and the form action. It does NOT land the home page content (slice W1 + T4 do that).
- **Slice W1 (`dashboard-svelte-skeleton`)** lands the `(app)/+layout.svelte`, Sidebar dynamic enumeration, `+error.svelte`, ConnectionIndicator, and the route placeholders for portfolio/trades/strategies (returning "loading…" until T4 fills them in). The kill-switch top-bar slot also lands here, even though K1 owns the underlying state — slot wiring is layout-level.
- **Slice T4 (`trading-routes-and-daemon`)** brings the `/portfolio`, `/trades`, `/strategies` routes online with full impl: tables, filters, force-exit, export.
- **SSE event contract** (per Architecture Decisions §API & Communication Patterns) — typed events `event:` + `id:` for resume + `data:` JSON. The reconnect indicator must show last-event-id during reconnect so the user can see catch-up progress.
- **CSV/PDF export** uses `Content-Disposition: attachment` with deterministic filenames `iguanatrader-trades-<tenant>-<from>-<to>.<ext>`.
- **CSP headers** set in `hooks.server.ts`: deny inline scripts (Svelte SSR safe); allow `connect-src` to API and SSE endpoints; allow TradingView Lightweight Charts CDN if used (TBD: bundle vs CDN).
- **A11y catch list (eslint-plugin-svelte)**: every `Input` paired with a real `<label>`; every icon-only `Button` carries `aria-label`; every interactive `Card` is `<button>` semantics; `+error.svelte` uses `role="alert"` for unrecoverable states.

## 9. Open questions

Resolved 2026-05-05:

- ✅ **Home summary** — state-only (equity, drawdown, top-3 approvals preview, risk caps, today's P&L). Tier-1/Tier-2 alerts surface via Sidebar badge counters; no inline "what just happened in the last hour" panel.
- ✅ **Force-create position UI (FR18)** — deferred post-MVP. Approval channel command remains the only entry point. Revisit when v2 expands the admin surface.
- ✅ **Per-tenant brand customisation** — not in MVP. v2 SaaS revisits (logo + accent shift on `(auth)/` and Sidebar header).
- ✅ **Trade detail surface** — drawer + URL query (modal-route hybrid). URL: `/trades?<filters>&detail=<trade_id>`. Preserves filter context, deep-linkable, back-button native. Validated mock at [variants/mock-j1-3-trade-detail.html](variants/mock-j1-3-trade-detail.html). Implementation lands in slice T4; a dedicated `/trades/[id]` route is NOT created.
- ✅ **YAML editor library** — CodeMirror 6 minimal build (~70 KB gzipped). Stack: `codemirror` + `@codemirror/lang-yaml` + custom theme derived from locked DESIGN.md tokens (accent=keys, success=strings, warn-bg=numbers, destructive=booleans, mute=comments). Lazy-loaded on modal open. Wrapped as `YAMLEditor.svelte` reusable primitive (props: `value`, `onChange`, `readonly`, `schema`) — future YAML surfaces (risk policy editor, methodology config) reuse it. Server-side schema validation is definitive; client-side basic lint is convenience only. Validated mock at [variants/mock-j1-4-yaml-editor.html](variants/mock-j1-4-yaml-editor.html). Monaco was rejected (~600 KB even YAML-only); plain textarea was rejected (no indent assistance, YAML breaks).

---

Status: **REFINED v1** (locked 2026-05-05 by Sally + Arturo). All TBDs closed; mocks for J1-3 + J1-4 in [variants/](variants/). Canonical for slice W1 (`dashboard-svelte-skeleton`) + slice T4 (`trading-routes-and-daemon`). Updates land via revision rows at the top of this file.
