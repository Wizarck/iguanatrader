# Generated by ai-playbook bootstrap. Append project-specific patterns above
# the playbook block; do NOT remove the playbook entries below or the auto-
# regenerated `.claude/injected-context.md` will start being committed.

# Project-specific (above the playbook block).

# Skills mirrors (single-source model, v0.17.0+): per-LLM mirrors are gitignored
# copies of .ai-playbook/skills/, materialised by scripts/materialise_skills.py.
skills/
.claude/skills/
.gemini/skills/
.skills-sources/

# Agent-spawned worktrees + scratch state (not project artifacts).
.claude/worktrees/
.claude/scheduled_tasks.lock

# Node — pnpm is the canonical workspace manager (per slice 1). npm/yarn
# competing lockfiles must NOT enter the repo.
node_modules/
package-lock.json
yarn.lock
.pnpm-store/

# Secrets — .env (plaintext) FORBIDDEN in repo; only .env.enc (sops-encrypted)
# may be committed. AGENTS.md §4 hard rule.
.secrets/*.env
!.secrets/*.env.enc
!.secrets/.sops.yaml
*.key
*.pem
*.p12

# IDE
.vscode/
.idea/
*.swp
*.swo

# Runtime data + logs
data/
logs/
*.db
*.sqlite
*.sqlite-journal
*.sqlite-wal
*.sqlite-shm

# Python — virtualenv + caches + build artefacts
.venv/
__pycache__/
*.pyc
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
.coverage.*
htmlcov/
coverage.xml
dist/
build/
*.egg-info/
.hypothesis/

# ai-playbook integration
.ai-playbook/overrides.log
.ai-playbook/hindsight-queue.jsonl
.ai-playbook/notifications.jsonl
.ai-playbook/heartbeat-t18a.txt
.claude/injected-context.md

# ai-playbook runtime state (telemetry, rule events, break-glass audit) — per-consumer, local only
.ai-playbook-state/

# >>> ai-playbook-config: per-consumer toggle state >>>
.ai-playbook/rules-toggle.json
.ai-playbook/rules-toggle-audit.jsonl
.ai-playbook/feature-flags.env
.ai-playbook/applied-config.json
.ai-playbook/applied-config.js
# <<< ai-playbook-config <<<

# Poetry: only the per-developer local override (.local) is gitignored; the
# committed `poetry.toml` carries the portable repo settings (e.g.
# virtualenvs.in-project = true) that ALL devs benefit from.

# === ai-playbook managed entries (do not remove) ===
.ai-playbook/.ai-playbook-state/
notifications.jsonl
hindsight-queue.jsonl
