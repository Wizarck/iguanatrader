node_modules
.svelte-kit
.env
.env.*
!.env.example
build
dist
*.tsbuildinfo
# Playwright artefacts (screenshots ARE checked in as visual baselines;
# the report + test-results dirs are not).
playwright-report
test-results
tests-e2e/.last-run.json
# Storybook build artefact (slice research-frontend-storybook). Local dev
# server runs without touching disk; only `pnpm build-storybook` writes.
storybook-static
