"""iguanatrader API package root.

Source layout: ``apps/api/src/iguanatrader/<package>/``. Each bounded context
lives under ``contexts/<name>/`` (slices 3+); the shared kernel is the only
top-level package added by slice 2 (``shared-primitives``).
"""
