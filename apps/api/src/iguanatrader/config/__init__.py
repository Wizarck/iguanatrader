"""Project-wide configuration helpers + boot-time validators.

Slice O1 (``observability-cost-meter``) plants the first module under
this package — :mod:`.settings` — to host the
``IGUANATRADER_DEV_INSECURE_COOKIE`` production guard (D9 carry-forward
item b). Future slices may add additional config validators here.
"""

from __future__ import annotations
