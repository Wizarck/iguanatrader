"""openbb-sidecar — AGPL-3.0 isolated OpenBB Platform service.

This package embeds the OpenBB Platform (AGPL-3.0-or-later) and exposes a
small HTTP surface consumed by the iguanatrader monolith over loopback.
The license boundary lives at the process + filesystem boundary; see
docs/adr/ADR-015 in the repo root for rationale.
"""

__version__ = "0.1.0"
